# UBER_LOG HANDOFF（開発引継ぎドキュメント）

更新日時: 2026-09-20 (JST)
対象プロジェクト: UBER_LOG (PWA / バニラJavaScript)
最新状態: 9/17スマホ配達明細復旧マイグレーション完了・検証PASS（HANDOFF READY）

---

## 1. プロジェクト概要・アーキテクチャ

UBER_LOGは、Uber Eats 配達員向けの実働・売上・配達効率・地雷スポット管理を行うモバイルファーストPWA（Progressive Web App）です。

- **技術スタック**:
  - フロントエンド: Vanilla HTML5 / CSS3 / ES6+ JavaScript（フレームワーク非依存、ビルド不要）
  - データ永続化: `localStorage`（主） + Supabase クラウド同期（`cloud-sync.js`）
  - デザイン思想: iPhone縦画面（幅375〜430px）最優先のハイコントラスト・ダークモダンUI（黒背景 `#0a0d12`、Uberグリーン `#06C167`、蛍光赤 `#ff453a`、アンバー `#f59e0b`）

- **主要ファイル構成**:
  - `index.html`: アプリケーションの基本骨格・4大タブ構造（今日・履歴・分析・地雷）および地図モーダル
  - `js/trip-maps.js`: Uber公式トリップ地図画像カタログ（`TRIP_MAP_CATALOG`）
  - `js/store.js`: データストア（公式シードデータ `CONFIRMED_SEED_DATA`、各種メトリクス計算、住所・日付フォーマッター、○／×評価および理由メモ管理）
  - `js/ui.js`: UIコントローラー（各タブの描画、カードレンダリング、地図モーダル、○／×評価トグル・理由メモ入力制御・トースト通知）
  - `js/cloud-sync.js`: クラウド同期・マージ制御（ローカルとクラウドの競合解決・確定値保護）
  - `css/app.css`: モバイルファーストスタイルシート（「引＝緑」「配＝赤」等の統一配色定義）
  - `assets/maps/`: 切り抜き済み公式実績地図（`map_*.png`）および元スクリーンショット（`full_*.png`）

---

## 2. データ思想と基本原則（最重要・絶対厳守）

1. **第1基準はUber公式明細・公式トリップ詳細（一次情報）**:
   - 売上・トリップ・報酬・距離・所要時間・地図は、Uber公式明細および公式スクリーンショットを唯一の真実（Source of Truth）とします。
   - 現場での手動タップ記録（「＋配達完了」等）は記憶補助・簡易ログであり、Uber公式実績に合わせて手動ログ側を修正・分離します（手動ログに合わせて公式データを改ざん・水増しすることは厳禁）。

2. **トリップ数と配達件数（ポイント）の厳格な概念分離**:
   - 1トリップで2件配達する「ダブル配達」が存在するため、「公式トリップ数（トリップカード数）」と「実配達件数（ポイント数）」は一致しない場合があります。
   - 例: 2026-09-19は **18公式トリップ（カード18枚） / 23配達（22pt）**。
   - 18件の明細カードを無理に23件に水増ししたり、架空の配達カードを生成することは絶対に禁止です。

3. **推測値・架空データの混入厳禁**:
   - 配達先住所や店舗名が不明な過去データについて、推測で架空の店舗や住所を捏造してはいけません。
   - 公式スクリーンショットが存在しないトリップに対して、Google Maps等で架空の地図ピンやルートを勝手に捏造してはいけません。

---

## 3. 直近の改修内容（9/17スマホ配達明細復旧 ＆ 履歴UI追加修正④）

### 【9/17スマホ配達明細復旧マイグレーション】
1. **9/17スマホ履歴欠落の原因**:
   - 実機スマホの `localStorage` に、過去に保存された空配列（`deliveries: []`）が残存していた。
   - `js/store.js` の起動時ローカルストレージ移行処理 `loadFromStorage()` において、9/14・9/15・9/16・9/18・9/19 には強制更新分岐が存在していたが、`2026-09-17` のマイグレーション処理のみが脱落していた。
   - その結果、`parsed.dailyLogs['2026-09-17']` が存在するためシード全体の代入はスキップされ、端末内の空配列キャッシュが上書きされず配達明細が0件（非表示）となっていた。
2. **9/17 migration追加**:
   - `js/store.js` の `loadFromStorage()` 内に `else if (date === '2026-09-17')` を追加。
   - スマホ側の `deliveries` が空、または現在の正式トリップ件数（7件）より不足している場合のみ、リポジトリに保存されている確定シードデータの7件で安全に更新し、即座に `localStorage` へ自動永続化するよう修正。
3. **既存7件を復旧するだけで、9/17公式金額の再監査は未実施**:
   - 今回の作業は「現在リポジトリに保存されている7件の配達明細をスマホでも表示できるようにする」ためのマイグレーション復旧のみに限定。
   - 9/17の金額・クエスト・日計等について過去記録との不一致の解消や、Uber公式明細・公式トリップに基づく再監査・再確定作業は実施していません（次回以降、一次資料に基づき必要に応じて実施）。

### 【履歴UI追加修正④】
1. **○／×評価理由の一言メモ（`evaluationReason`）の追加**:
   - **表示制御**: 未評価時は入力欄を完全非表示（カードをスッキリ維持）。○または×を選択した時のみ、カード下部に1〜2行のコンパクトな入力欄が展開。
   - **プレースホルダー**: 
     - ○選択時: `○の理由（例: 店も配達先も楽、高単価）`
     - ×選択時: `×の理由（例: 入館ロス、大迂回、トンネル）`（枠線フォーカスも警告赤系へ変化）
   - **誤操作保護（非破壊設計）**:
     - 誤タップで未評価に戻した場合、入力欄は非表示になりますが、入力済みの理由は即座に破棄されず内部に保持。
     - 再度○または×をタップした際、直前の理由メモが即座に復元。
     - 明示的に文字を削除した場合のみ空文字として保存。
2. **既存詳細メモ（`del.memo`）との完全分離**:
   - 過去に記録された現場経験メモ（例: 「福島タワー 入館管理の手続きが複雑……」「43号線付近で自転車ルート判断を誤り……」）は `del.memo` として完全保護・独立表示。
   - 今回追加した `evaluationReason` と混同・自動コピー・上書きは一切行いません。
3. **評価および理由メモの永続保存アーキテクチャ**:
   - `localStorage`（キー: `uber_log_trip_evaluations`）に以下の構造で保存:
     ```json
     {
       "del_0919_1": {
         "evaluation": "OK",
         "reason": "店も配達先も楽",
         "updatedAt": "2026-09-20T01:34:00.000Z"
       }
     }
     ```
   - 公式実績本体とは完全に分離された「利用者体験データ」として保存され、ブラウザ再読込・PWA再起動後も完全維持。
4. **「引＝緑」「配＝明るい赤」の配色ルール統一**:
   - `引`（引き取り先/ピックアップ）: 緑（`var(--color-uber-green)` `#06C167`）
   - `配`（配達先/ドロップ）: 明るい蛍光感のある赤（`#ff453a`、背景 `rgba(255, 69, 58, 0.18)`、枠線 `rgba(255, 69, 58, 0.45)`）
   - ダークテーマ（`#0a0d12`）でも一目で識別できるハイコントラストを確保。地図表示・ルート表示の全UIでルールを統一。
5. **各トリップカードへの「地図」ボタン新設**:
   - 各公式トリップカードのフッター（距離・所要時間横）にコンパクトな `🗺️ 地図` ボタンを配置。
   - 普段は地図を非表示にし、履歴一覧が縦長に肥大化するのを防止。
6. **Uber公式スクリーンショットを利用した地図モーダル表示**:
   - `🗺️ 地図` を押した時だけ、専用モーダル（`#trip-map-modal-overlay`）がポップアップ。
   - **地図部分の正確な自動切り抜き**: 元スクリーンショットから安全に特定された地図矩形（約430×233px）を表示。
   - **元スクショ全体フォールバック**: モーダル内の「🔍 元スクショ全体を表示」ボタンで、いつでも切り抜きと元画像全体をトグル切替可能。
   - **架空地図の生成禁止**: 公式スクリーンショットが存在しないトリップでは「公式地図画像は未登録です（推測地図は生成しません）」と明示し、架空ピン・推測地図は一切生成しません。
7. **公式スクリーンショットとの紐付け状況**:
   - **9/19**: 全18トリップすべて紐付け完了・切り抜き画像生成済み（100%）。
   - **9/18**: 全9トリップすべて紐付け完了・切り抜き画像生成済み（100%）。
   - **9/17**: 基準事例（del_0917_7 モスバーガー市岡店）紐付け完了。その他の過去トリップは元画像未登録のため未対応（安全に未登録通知）。
   - **9/16**: 基準事例（del_0916_8 ガスト九条店 ¥1,050）紐付け完了。
   - 合計 **29件** の公式実績地図が `assets/maps/` および `js/trip-maps.js` に登録済み。
8. **将来のAI分析機能の扱い**:
   - 将来的に「○/×」「評価理由」「詳細経験メモ」「店舗」「配達先」「距離・時間・報酬」からAIで傾向パターン抽出（「トンネル経由の×が多い」「この建物は×が多い」等）を行うためのデータ構造として設計済み。
   - ※指示に基づき、現時点では自動判定・自動評価・地雷自動登録機能は**未実装（意図的保留）**です。

---

## 4. 現在のUI状態と各タブの画面仕様

### ① 【今日】タブ（配達中・実稼働メイン）
- **上部ヘッダー**: `UBER LOG` + 簡潔日付 `9月19日（土）` + 設定⚙️
- **今日のメインサマリー**: 配達件数（巨大数字）、実働時間、直前記録取消ボタン、稼働制御ボタン（休憩・終了）
- **配達完了記録ボタン**: 「＋ 配達1件完了」
- **本日の配達リスト**: タイトル `9月19日（23件 / 18トリップ）`、18枚の公式トリップカード（タップで詳細モーダル起動、カード右下に○／×ボタン、評価時のみ理由入力欄展開、🗺️ 地図ボタン）
- **昨日の実績比較**: `23件` / `8,858円`

### ② 【履歴】タブ（日別履歴）
- **タイトル**: `📅 日別履歴（26/9/10～9/19）`
- **アコーディオン式カード**:
  - 見出し: `9月19日（土）`（カレンダー絵文字なし） + 配達23件 / 日計¥8,858 / 通常報酬¥7,908 / クエスト¥950
  - ボーナスバッジ（ある日のみ）: `ボーナス（新規保証） +¥12,132（総額 ¥20,990）`
  - メトリクスストリップ: `走行 68.2km / 実走 8時間30分 / 時給 ¥1,042`（数字は鮮明な白文字）
  - 配達明細見出し: 白文字14px太字（`配達明細（全18トリップ / 23件）`）
  - 公式トリップカード:
    - 控えめな `#1` 表示、右上に大きな報酬金額
    - `引`（緑）+ 店舗名（折り返し対応）
    - `配`（明るい赤）+ 配達先（大阪市省略）
    - 距離・時間表示 + ダブル配達バッジ（左側）
    - `🗺️ 地図` ボタン（押すと公式地図モーダル起動）
    - ○／×評価トグルボタン（右側）
    - ○または×選択時のみ展開する評価理由一言メモ入力欄（未評価時は非表示）
    - 既存詳細経験メモ（ある案件のみ独立表示）

### ③ 【分析】タブ（売上・配達効率分析）
- **ヒーローカード**: 今週の売上 `¥31,005`（期間: `26/9/14～9/20`）
  - 内訳: 通常報酬 `¥27,009` / クエスト `¥3,800` / 配達件数 `64件`（57トリップ）
- **今月の売上**: `¥34,069` / 72件
- **累計パフォーマンス**: 累計配達 72件 / 累計売上 ¥34,069 / 平均単価 ¥473 / 総走行距離 232.7 km
- **日別実績テーブル**: 9/19〜9/14の降順テーブル

### ④ 【地雷】タブ（原則回避データベース）
- **回避方針**: 原則回避・要検証スポット、実走基準事例（9/17 モスバーガー、9/19 福島タワー、9/19 スシロー辰巳橋店）

---

## 5. 公式確定実績データ一覧（2026-09-10 〜 2026-09-19）

| 日付 | 実配達 | 公式トリップ | 通常報酬 | クエスト | 調整金 | 当日総額 | 自転車代 | 差引手取 | 走行距離 | 地図登録 |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **9/10** | 3件 | 2件 | ¥1,103 | -- | -- | ¥1,103 | -- | ¥1,103 | 14.31km | 未登録 |
| **9/11** | 5件 | 4件 | ¥1,961 | -- | -- | ¥1,961 | -- | ¥1,961 | 17.47km | 未登録 |
| **9/14** | 4件 | 4件 | ¥2,788 | -- | -- | ¥2,788 | -- | ¥2,788 | 18.02km | 未登録 |
| **9/15** | 12件 | 11件 | ¥5,584 | ¥500 | -- | ¥6,084 | -- | ¥6,084 | 44.41km | 未登録 |
| **9/16** | 8件 | 8件 | ¥4,821 | ¥1,350 | -- | ¥6,171 | -- | ¥6,171 | 37.01km | 基準1件 (del_0916_8) |
| **9/17** | 6件 | 7件 | ¥3,991 | ¥1,000 | -- | ¥4,991 | -- | ¥4,991 | 5.58km | 基準1件 (del_0917_7) |
| **9/18** | 11件 | 9件 | ¥3,917 | ¥950 | +¥200 | ¥5,067 | -¥1,980 | ¥3,087 | 33.24km | **全9件登録** |
| **9/19** | 23件 | 18件 | ¥7,908 | ¥950 | -- | ¥8,858<br>(+保証¥12,132=¥20,990) | -- | ¥20,990 | 68.24km | **全18件登録** |

---

## 6. 残課題・未着手機能（次工程ロードマップ）

利用者の指示により、以下の新機能は**現時点では実装が意図的に凍結・保留**されています。
次のAIは、利用者から明示的な着手指示があるまで、これらの機能を勝手に実装してはいけません。

1. **AIによる○／×・理由メモ・ルート自動分析**:
   - 蓄積された○/×評価、理由メモ、店舗、配達先、距離、時間等のデータからAIが自動で地雷候補や高効率パターンを抽出する機能（未着手・データ構造のみ準備完了）。
2. **地雷リスト／攻略マップへの自動ピン留め**:
   - ×評価や理由メモから地雷DBへ自動投入する機能。
3. **未登録過去トリップの地図画像拡充**:
   - 9/10〜9/15等の過去トリップについて、利用者から追加資料の提供があった場合に紐付けを拡充。

---

## 7. 次のAIへの引き継ぎ注意事項

1. **公式実績データの改変禁止**:
   - `CONFIRMED_SEED_DATA` 内の公式トリップ数、報酬額、距離、所要時間、日次確定値は一次情報に基づき確定しています。絶対に改変・推測補完を行わないでください。
2. **架空地図の生成禁止**:
   - 公式スクリーンショットが存在しないトリップに対して、Google Maps等で架空の地図ピンやルートを勝手に捏造してはいけません。
3. **既存詳細メモと評価理由メモの混同禁止**:
   - `del.memo`（詳細経験知）と `evaluationReason`（一言理由メモ）は別物です。絶対に上書き・マージしないでください。
4. **「引＝緑」「配＝赤」の厳守**:
   - 配達先バッジおよび今後の地図表現では `引＝緑`、`配＝明るい赤` を必ず守ってください。
