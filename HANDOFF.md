================================================================================
UBER_LOG 正本開発引継ぎドキュメント（MASTER HANDOFF）
================================================================================
最終更新日時: 2026-09-27 (JST)
対象プロジェクト: UBER_LOG (PWA / バニラJavaScript)
正本ファイル: C:\Users\okano\Desktop\UBER_HANDOFF.txt
最新状態: v38（2026-09-27 公式取込ツールのMAP検出を表示倍率の違うスクショに対応。アプリ本体は v38 のまま）。バイクシェア1日パスの自動入力額を ¥1,527 に正式統一（v32 の「¥527 が正しい」は誤設定）、日別履歴の特別ボーナス系アイコンを 🏆 に統一（「賞」廃止）、稼働日の共通判定（isWorkedDay）で非稼働日を日別履歴・日別比較から除外。v34 の同期修正の実環境確認はユーザー操作待ち。
（以下 v37 時点の状態記述）v37。特別クエストを平均日給・平均単価・通常時給などの効率指標から除外（大型特別ボーナスと同じ扱い。総売上・利益・特別ボーナス集計には含める）。v34 の同期修正の実環境確認はユーザー操作待ち。
（以下 v36 時点の状態記述）v36。分析の売上内訳で特別クエスト（80回乗車クエスト ¥8,890）を「特別ボーナス」に分類（9/21〜: クエスト ¥4,650 / 特別ボーナス ¥8,890 / 総売上 ¥41,405 不変）。v34 の同期修正の実環境確認はユーザー操作待ち。
（以下 v35 時点の状態記述）v35。日別一覧に特別クエストの日の 🏆（賞と同じ金色系）を追加し、特別クエスト金額を「+¥」表記に統一。v34 の同期修正の実環境確認はユーザー操作待ち。
（以下 v34 時点の状態記述）v34。起動時にクラウド同期が実行されずスマホ⇔Web で経費が共有されなかった不具合を修正し、同期状態（ヘッダーのドット・未ログイン／ファイル版の注意）を表示。実環境での同期確認はユーザー操作待ち。
（以下 v33 時点の状態記述）v33。2026-09-24 を反映（16trip / 22件 / ¥17,189・クエスト ¥9,490 のうち特別クエスト 80回乗車 ¥8,890・MAP 16/16）。取込の重複スクショ対応・クエストの達成表示と売上計上の統合・特別クエスト（🏆）対応。
（以下 v32 時点の状態記述）v32。経費入力でバイクシェアを選んだ時の自動入力額を ¥527 に修正。v27 の経費消失修正はスマホ実機で確認済み。
（以下 v31 時点の状態記述）v31。履歴カレンダーの表示改善（説明文をカード外・16px、金額は100円未満切り捨て・カンマなし、日曜・祝日を赤系表示）、日本の祝日を年ごとに計算、経費入力の初期値を「必要経費」に（バイクシェア選択で1日パスを自動入力。金額は v32 で ¥527 に修正）。集計は変更なし。v27 の経費消失修正は v32 時点でスマホ実機確認済み。
（以下 v30 時点の状態記述）v30。履歴カレンダーの説明文を大きく簡潔にし、非稼働日（9/12・9/13・9/20）を一覧から除外（カレンダーでは「休」）。v27 の経費消失修正は v32 時点でスマホ実機確認済み。
（以下 v29 時点の状態記述）v29。日別履歴で非稼働日の「利益 --」を非表示にし、［一覧］［カレンダー］切替を追加（カレンダーは稼働日の件数・売上と、確認済み非稼働日 9/12・9/13・9/20 の「休」）。データ・集計は変更なし。v27 の経費消失修正は v32 時点でスマホ実機確認済み。
（以下 v28 時点の状態記述）v28。チップを独立項目として表示・集計（履歴の♥・日別詳細のチップ・今週内訳のチップ／調整の独立・期間指定の売上内訳）。総売上・利益はチップの二重計上なしで従来どおり。v27 の経費消失修正は v32 時点でスマホ実機確認済み。
（以下 v27 時点の状態記述）v27（緊急修正・スマホ実機確認済み）。スマホで登録した経費が後から消える問題を修正（端末保存・クラウド送信の丸ごと上書きを日ごと・経費ID単位の統合に変更）。公式データ・MAPは変更なし。
（以下 v26 時点の状態記述）v26。毎日の公式取込を「デスクトップの UBER_LOG → 取込画面 →［取り込み実行］」の1画面・1ボタンに統合（ローカルbackend＋非対話のスクショ読取＋既存パイプライン＋HANDOFF自動記録＋commit/push）。公式データ・MAPは変更なし。
（以下 v25 時点の状態記述）v25。履歴Bバッジ修正（アプリで登録した「バイクシェア」経費で B が付かなかった問題を共通判定化で解消）＋ UBER取込.cmd（ダブルクリックで最新日付の /uber-import を自動開始）を追加。公式データ・MAPは変更なし。
（以下 v24 時点の状態記述）v24。2026-09-23 の公式実績を公式取込v1（/uber-import）で初めて取込（20trip / 23件 / 総売上 ¥10,683 / MAP 20/20、VALIDATION PASS）。取込時に MAP切り抜きの検出改善（縦の白い道路で途切れる地図）・一覧の小数表記（￥750.00）対応を実施。9/23の経費はアプリ入力分のため HANDOFF未把握。
（以下 v23 時点の状態記述）v23リリース。「UBER_LOG 公式取込 v1」を実装。毎日「一覧テキスト貼付＋Delivery詳細スクショ投入＋ /uber-import YYYY-MM-DD」だけで、照合・検証・反映・テスト・HANDOFF・commit/push まで進む。実績データ（9/22まで）は変更なし。
（以下 v22 時点の状態記述）v22リリース。Uber公式明細で確認された 2026-09-22 23:46「調整」Support Adjustment -¥607 を売上調整（分析上「その他」）として正式追加。9/22総売上 ¥9,850 → ¥9,243、9/22利益 ¥8,323 → ¥7,716。Delivery tripは一切変更なし。経費ではない。80件クエストは35/80のまま。
（以下 v21 時点の状態記述）v21リリース。2026-09-21・09-22のUber公式確定実績（25公式トリップ／35配達）を公式スクリーンショット照合のうえ追加。クエスト重複表示の二重計上排除、チップ¥50の内訳保持、Bike経費 各日¥1,527登録、80件クエスト進捗 35/80（残45）補正、公式MAP 25件追加（累計89/89件=100%）。v20の「稼働」名称変更＋経費入力機能はそのまま継続。既存確定Bikeデータ（9/18 ¥1,980、9/19 ¥1,527）と9/19 ¥320修復結果は保護済み。

■ 公式取込ツール修正（2026-09-27・アプリ版は v38 のまま）: MAP検出を表示倍率の違うスクショに対応
  - 症状: 9/26 の Delivery詳細スクショ25枚（横幅 374〜469px）が取込画面で全件「地図が画像の端で切れています」。
  - 原因: crop_map.py が地図を 420x233（幅 420±3px）固定で探していた。9/26 のスクショは表示倍率が違い、地図は 378x210（縦横比は同じ 420:233）。
    さらに地図の上に「見積もり料金」の灰色の枠があり、233行の窓がその枠から始まって下端判定に失敗していた。
  - 修正: 420x233 固定判定（従来どおり・既存MAPの切り抜き位置は不変）で見つからない場合だけ、_detect_scaled で検出。
    高さ（上下の白い余白に挟まれた範囲）と幅を別々に測り、①上下が白 ②左右に白い列が1列以上（画像端に接していない）③幅＝高さx420/233（±2px）
    ④塗り率 MIN_FILL 以上 をすべて満たす場合だけ採用。切り抜きは拡大縮小せず元の大きさのまま保存（アプリは幅100%表示）。
  - 9/26 の結果: 23/25 を 378x210 で検出。060616（地図 373x210）・061603（地図 376x210）は地図の右端が画像の右端に接しており（白い余白なし）、
    他の23枚の 378px より 5px・2px 欠けている＝本当に切れているため不合格のまま（緩和しない）。撮り直すか、MAPなしで良ければ mapMissingOk。
  - 既存 full_*.png は全件、従来と同じ判定結果（新判定を単独で当てても従来より厳しく、従来NGの画像を通すものはなし）。
  - 変更ファイル: tools/official-import/lib/crop_map.py / tools/official-import/spec/run-tests.js（3-2 追加）/ tools/official-import/README_JA.md
  - テスト 274項目 全PASS（3-2: 0.9倍表示の画面で 379x210 を検出、右4px・右2px・左3px欠け、右端ちょうど、下端欠け、縦横比不一致はすべて不合格）。

■ v38（2026-09-25 追加修正C書）: バイクシェア初期値 ¥1,527 正式統一・🏆 統一・稼働日の共通判定
  1. バイクシェア1日パスの自動入力額 ¥1,527（正式仕様）
     - 経費入力で種類「バイクシェア」を選ぶと 内容「バイクシェア1日パス」・金額「1527」を自動入力（手動変更可）。スマホ・PC・公開版・file:// 共通（js/ui.js の1か所）。
     - 【訂正】v32 の記録「¥527 に修正（v31 の 1527 は誤り）」および v34 項目5 の「527」は誤設定だった。正しい初期値は ¥1,527。過去の記録は履歴として残し、本版で訂正する。
       原因: v32 で入力フォームの初期値を 1527 → 527 に変更したこと（保存・集計ロジックの不具合ではない）。
     - 変更は入力フォームの初期値だけ。登録済みの経費データ（¥527 で登録済みの分を含む）は書き換えない。保存・修正・削除・同期ロジックは変更なし。
  2. 日別履歴の特別ボーナス系アイコンを 🏆 に統一
     - 新規保証・特別収入（guaranteeBonus。例: 9/19 ¥12,132）と特別クエスト（questType: special。例: 9/24 80回乗車 ¥8,890）のどちらか／両方がある日に 🏆 を1個だけ表示。
       日別属性の「賞」は廃止（属性キー special_bonus のラベルを 🏆 に。旧 special_quest 属性は統合して削除）。配色は従来の金色系（attr-bonus）のまま。並び順 B → 調 → 🏆 → ♥。
     - 見た目のアイコンだけの統一。内部データは guaranteeBonus / specialQuestSales で別管理のまま。日別詳細は本来の名称（「特別収入（新規保証）」「特別クエスト」）で表示し、カードのアイコンも 🏆。
       分析の売上内訳は従来どおり両方を「特別ボーナス」に集計（v36）。
     - 表示: 9/19 [B][🏆]・9/24 [🏆]（端末に Bike 経費があれば [B][🏆]）。
  3. 稼働日の共通判定 store.isWorkedDay（日別履歴一覧・分析の日別比較／稼働日数・カレンダー・売上内訳の「稼働 N日」で共通）
     - 稼働日 = 公式の配達件数（deliveriesCount）またはトリップ数（tripsCount）が1以上 / 実配達（店舗名または報酬額のある Delivery）が1件以上 / 配達売上 > 0 のいずれか。
     - 稼働日ではない: 稼働開始時刻だけ・workSession だけ・手動タップ（店舗名も報酬もない空タップ）だけ・経費だけ・同期データだけ・空レコード・0件/売上0。
       「今日だから」「dailyLogs にレコードがあるから」では表示しない。今日に配達実績が1件入れば自動で表示される。
     - カレンダーは getDayStatus（worked = isWorkedDay / off = CONFIRMED_DAY_OFF_DATES または dayOff / それ以外は空欄）で、9/12・9/13・9/20 は「休」のまま。
     - 9/20 が日別比較に残っていた原因: getAnalytics の日別比較・稼働日数が「件数 > 0 または 稼働開始時刻あり または 売上が null でない」で判定しており、
       端末に 9/20 の稼働開始時刻（workStartedAt）が残っていると 0件 / -- / 未記録 の行として表示されていた（一覧側は v30 で「休」の日を除外済みだったが別の判定だった）。
       9/20 のハードコードではなく共通判定で除外。9/20 の日別データ・稼働開始時刻・workSession は削除せず、表示判定だけで除外。
     - 既存稼働日（9/10〜9/24 の12日）の件数・売上・利益・時給・平均日給 ¥5,634・平均単価 ¥445・総売上 ¥88,630 は不変（シードのみで v37 と全値一致を確認）。
  - 変更ファイル: js/ui.js（初期値・一覧の判定・詳細カードのアイコン）/ js/store.js（属性・isWorkedDay・getDayStatus・getAnalytics・getSalesBreakdown の日数）/
    tools/official-import/spec/run-tests.js（10-7 更新・10-10・10-11 追加）/ version: 20260925_v38
  - テスト 267項目 全PASS（10-7: 9/19 🏆・9/24 🏆・同日両方でも1個・「賞」なし / 10-10: 初期値1527・手動変更・既存経費不変 /
    10-11: 9/20・9/12・9/13 を日別比較に出さない・カレンダー「休」・9/20データ保持・今日の空タップは非表示・実配達で表示・経費/空/workSessionだけは非稼働・既存値不変）。

■ v37（2026-09-25）: 効率指標から特別クエストを除外
  - 通常分析売上（平均日給・平均単価・通常時給・日別比較の通常売上／時給の基準）= 配達報酬 ＋ 通常クエスト ＋ 調整等。
    大型特別ボーナス（新規保証等）に加えて、特別クエスト（questType: special）も除外。通常クエスト（normal）は従来どおり含む。
    総売上・売上利益・特別ボーナス集計（分析の内訳）には従来どおり含める。
  - 実装: getCalculatedMetrics の hourlyBaseSales（= regularSales）から specialQuestSales を差し引き、getAnalytics の通常分析売上合計もこの値を使用。
  - 値の変化（9/24 まで）: 通常分析売上 ¥76,498 → ¥67,608 / 平均日給 ¥6,375 → ¥5,634（12日）/ 平均単価 ¥503 → ¥445（152件）/ 平均時給 ¥1,431 → ¥1,264 /
    9/24 通常時給 ¥2,566 → ¥1,239（日別比較表示 ¥1,240）。総売上 ¥88,630・今月売上利益 ¥82,069・今週 ¥41,405 / ¥38,351・特別ボーナス ¥8,890 は不変。
  - 変更ファイル: js/store.js / tools/official-import/spec/run-tests.js（10-9）/ version: 20260925_v37
  - テスト 248項目 全PASS（10-9: 9/24 の通常分析売上 ¥8,299・通常時給、9/23 は変化なし、9/19 の大型特別ボーナス除外は従来どおり、今月の平均日給・平均単価、総売上・利益・特別ボーナスは不変）。

■ v36（2026-09-25）: 分析の売上内訳で特別クエストを「特別ボーナス」に分類
  - 分析（今週の売上利益の内訳・売上内訳（期間指定））では questType: normal → 「クエスト」、questType: special → 「特別ボーナス」に分類（二重計上なし）。
    既存の特別ボーナス（9/19 新規保証 ¥12,132 等の特別収入）の集計はそのままで、そこへ特別クエストを加算。
    getRevenueSummary().thisWeek / getSalesBreakdown() の questSales は通常クエストのみ、guaranteeBonus は特別収入＋特別クエスト。
    内訳用に guaranteeOnlySales（特別収入・保証）/ specialQuestSales（特別クエスト）も追加。総売上・売上利益は変わらない。
  - 日別（getCalculatedMetrics の questSales）と日別詳細の表示は変更なし（9/24 はクエスト合計 ¥9,490・🏆 80回乗車クエスト +¥8,890 特別クエスト（クエストに含む））。
    役割: 日別詳細 = クエスト全体として把握 / 分析 = 通常クエストと特別ボーナスを分けて分析。
  - 2026/9/21〜9/25: クエスト ¥4,650 / 特別ボーナス ¥8,890 / 総売上 ¥41,405（変更なし）。今月: 特別ボーナス ¥21,022（¥12,132 ＋ ¥8,890）。
  - 平均日給・平均単価・時給など効率指標の「通常分析売上」はこの版では変更していない（→ v37 で特別クエストを除外することに決定・実装）。
  - 変更ファイル: js/store.js（週次・期間指定の集計）/ tools/official-import/spec/run-tests.js（10-8）/ version: 20260925_v36
  - テスト 239項目 全PASS（10-8: 今週・期間指定・9/24・9/23・9/19・今月の分類、どの期間も内訳合計＝総売上、既存特別収入と確定週 ¥43,461 不変、日別は ¥9,490 のまま）。

■ v35（2026-09-25）: 日別一覧の 🏆（特別クエストがあった日）・特別クエスト金額の「+¥」表記
  - 日別属性に special_quest（ラベル 🏆）を追加。questType が special のクエストが1件以上ある日だけ表示（複数あっても1個）。通常クエストだけの日は表示しない。
    配色は既存の「賞」（attr-bonus）と同じ金色系を使用（新色なし・サイズ24px四角）。並び順 B → 調 → 賞 → 🏆 → ♥。例: 9/24 は [B][🏆]（端末に Bike 経費がある場合）。
  - 日別詳細の特別クエストカードの金額を「+¥8,890」表記に（9/19 の特別収入「+¥」と統一）。稼働画面のクエスト明細は従来から計上クエストを「+¥」表示のため変更なし。
    通常クエストの表示・金額計算・保存データは変更なし。
  - 変更ファイル: js/store.js（属性定義・判定・並び順）/ js/ui.js（カード金額表記）/ css/app.css（🏆 の大きさ）/ tools/official-import/spec/run-tests.js（10-7）/ version: 20260925_v35
  - テスト 230項目 全PASS（10-7: 9/24 に 🏆 1個・賞と同じ配色・特別クエストの日だけ・複数でも1個・通常だけの日は無し・並び順・既存 9/19「B 賞」9/22「B 調 ♥」不変）。

■ v34（2026-09-25 追加修正C書 D）: スマホ⇔Web の経費同期の不具合修正・同期状態の表示
  1. 構成（正本）:
     - 経費などユーザー入力の正本は Supabase のクラウド表 uber_daily_logs（ユーザーごと・日付ごとの日別データ）。各端末の localStorage は端末内の写し。
       公式データ（trip・売上・クエスト・MAP）は js/store.js の同梱データ（公式取込で更新）を正とし、起動時に端末データへ同期。
     - 端末（スマホのホーム画面アプリ／PCブラウザの github.io／PCで index.html を直接開いた file://／PCの localhost:8088）は、ブラウザ上それぞれ別の保存場所。
       端末間で経費が共有されるのは、それぞれがクラウド同期にログインしていて同期が実行された時だけ。
  2. 根本原因（経費がスマホと Web で分かれた）:
     - 起動時にクラウド同期が一度も実行されていなかった。Supabase の接続（保存済みログインの復元）は isReady()/getSession() が呼ばれた時にだけ行われるが、
       起動時にそれを呼ぶ処理が無く、同期（pullAndSync）は画面切替（visibilitychange）・オンライン復帰・ログイン操作の時にしか始まらなかった。
     - 保存後の自動送信（pushPendingChangesInternal）は isLoggedIn() が false（ログイン未復元）だと何もせず終了していた。
       → アプリを開いて経費を入力し、そのまま閉じると、ログイン済みでもクラウドへ送られず（未送信のまま）、他端末も開いただけでは取得しなかった。
     - さらにヘッダーの同期ドット（⚙️の横）は常に緑で、同期状態を表示する要素（sync-status-badge）は画面から無くなっていたため、未同期・未ログインに気づけなかった。
     - ユーザーの「Web版」は index.html をファイルとして直接開いたもの（file://）。スマホ（github.io）とは別の保存場所で、自動更新（版の確認）も動かない。
       PC の Edge の保存データには file:// のクラウド同期の痕跡（初回移行済み・未送信日付・Supabase のロック）があり、Web 側は過去にログイン済みと推測される。
       スマホ側のログイン状態は PC からは確認できない（ユーザー確認待ち）。
  3. 修正:
     - js/cloud-sync.js: 起動時に1回同期（保存済みログインを復元 → Pull → 未送信分を Push）。表示中は5分ごとにも同期。
       送信前にログイン（セッション）を復元してから送る（未ログインなら未送信のまま残し、ログイン後の同期で送る）。
     - js/app.js・index.html・css/app.css: ヘッダーの同期ドットを実際の状態で表示（緑=同期済み／青=同期中／赤=エラー／灰=未ログイン・オフライン・未設定）。
       未ログインの時は「クラウド同期に未ログインです。この端末で入力した経費などは、ほかの端末に反映されません［ログイン］」をヘッダー下に表示。
       ファイルとして開いている時は「自動更新されません。PCでは公開版 okachan1973-navi.github.io/uber-log を使ってください」を表示。
  4. 🏆 特別クエストが見えなかった件:
     - 公開中の GitHub Pages（v33）と localhost を新しいブラウザで開いて確認したところ、9/24 の日別詳細に「🏆 80回乗車クエスト ¥8,890 特別クエスト（クエストに含む）」が表示された（配信コード・データは正しい）。
     - Web（file://）は自動更新されないため、v33 以前に開いたままのタブ・再読み込み前の画面では表示されない。スマホも更新前の版を表示していた可能性。
       → 再読み込み（アプリの再起動）で v34 に更新されることを確認してもらう。稼働画面のクエスト明細は「今日」のクエストのみ表示（9/24 分は日別詳細で確認）。
  5. バイクシェア自動入力（v32 仕様・今回変更なし）: 種類「バイクシェア」選択で 内容「バイクシェア1日パス」・金額「527」を自動入力（手動変更可）。同期不具合とは無関係。
  6. 9/24 の経費: 端末ごとの保存データのため、PC から直接の確認・削除はしていない（ユーザー操作で実施）。
     - スマホの Bike Share 経費は、スマホがログイン済みなら v34 で起動時に自動送信される。Web の「テスト用 1円」は Web の経費一覧から削除する（削除は tombstone として全端末に同期）。
  7. テスト（node tools/official-import/import.js test: 224項目 全PASS）: spec/sync-tests.js 14-7 を追加
     （本物と同じく保存済みログインを遅れて復元する端末で: 開いただけで同期・スマホ入力が自動送信・Web を開くと表示と B・Web 入力がスマホに表示・二重登録なし・
       9/24 の特別クエスト/クエスト/Delivery 維持・未ログイン端末は送信せず「未ログイン」表示・未送信として保持）。修正前のコードでは同じテストが 6項目 FAIL。
  8. 変更ファイル: js/cloud-sync.js / js/app.js / index.html / css/app.css / tools/official-import/spec/sync-harness.js・sync-tests.js / version: 20260925_v34

■ v33（2026-09-24分 修正C書）: 9/24 取込の照合不一致・クエスト重複判定の修正、特別クエスト対応、9/24 反映
  1. 原因（9/24「取り込み実行」で 一覧16 / スクショ16 / 照合14・trip合計 ¥6,518 となり停止していた件）:
     - 照合ロジック（日付・時刻・金額の完全一致）は同額の別Deliveryを正しく区別しており、17:39 ¥320 と 18:09 ¥320 も別扱いだった。
     - 実際の原因は保存されたスクショの組み合わせ: ① 07:42 ¥861 のスクショ（203331.png）が3回目の実行時に画面の × で外され _previous へ退避されていた
       ② 18:09 ¥320 を2回撮ったスクショ（203955.png と撮り直し 205021.png）が両方入っていた（一覧1件に対しスクショ2枚で件数不一致）。
       差額 ¥1,181 = 07:42 ¥861 ＋ 18:09 ¥320（照合できなかった2件）。
     - クエストは、一覧で 19:15「6 回乗車クエスト ¥600」「80 回乗車クエスト ¥8,890」（達成表示）と 19:23「クエスト ¥600」「クエスト ¥8,890」（売上計上）が別時刻で出ており、
       従来の「同時刻・同額の組だけ統合」ルールでは4件計上になり、アプリの重複判定と矛盾して停止していた。
  2. 修正（tools/official-import）:
     - lib/activity-parser.js: 達成表示（N回乗車クエスト）→ 売上計上（クエスト）が同じ日・同じ金額・0〜60分後の組を同一報酬として1回計上（1対1・最も近い時刻）。
       金額だけでは統合しない（時刻が離れすぎ・金額違い・計上が達成より前は統合しない）。計上する行に questName（回数クエスト名）・questType・achievedAt を付与。
       特別クエスト判定: 20回以上の回数クエスト（SPECIAL_QUEST_MIN_TRIPS=20。例: 80回乗車クエスト）→ special、それ以外（6回・3回など）→ normal。
     - lib/pipeline.js: 同じDeliveryの重複スクショ（時刻・金額・時間・距離・ポイント・店舗・配達先・チップがすべて一致）は1枚として扱い注意に記録。
       重複スクショがある場合は地図が切り抜けた方を MAP に使う（origFile もその画像）。中身が違う同時刻・同額のスクショは従来どおり停止（安全装置は維持）。
       新規クエストに questName / questType / achievedAt を保存し、達成時刻と計上時刻が違う組の note を記録。
  3. 特別クエストの保存仕様（既存の quests[] を拡張。新しい別構造は作らない）:
     { id, time（売上計上時刻）, title:"クエスト", amount, isDuplicateIgnored:false, questName:"80回乗車クエスト", questType:"special"|"normal", achievedAt:"19:15"（達成時刻・計上と違う場合）, note }
     sales.quest には通常・特別を合算（会計上はクエスト報酬）。既存日のクエスト（questType なし）は従来どおり通常扱い。
  4. 表示（js/store.js・js/ui.js・css/app.css）:
     - getCalculatedMetrics に specialQuests / specialQuestSales（クエスト報酬の内訳。別途加算しない）を追加。
     - 日別詳細に「🏆 80回乗車クエスト ¥8,890 特別クエスト（クエストに含む）」カード（賞と同じ金色系）。稼働画面のクエスト明細は 🏆＋「特別」バッジ、通常は 🎁＋クエスト名。
  5. 9/24 データ（公式取込で反映。二重登録なし）:
     - inbox の 07:42 スクショを _previous から screenshots へ戻し（コピー。退避分は残置）、読取値（07:42 ¥861・41分27秒・6.82km・2pt・魚屋のおむすび丸徳・大阪市浪速区稲荷2丁目）を記入。
     - 反映前のバックアップ: scratch/backup_20260924_before_fixC/（inbox・staging・HEAD の store.js / trip-maps.js）。
     - 2026-09-24 (木): 配達 22件 (16トリップ) | 配達報酬 ¥7,699 ＋ クエスト ¥9,490（通常 6回乗車クエスト ¥600 ＋ 特別 80回乗車クエスト ¥8,890）＋ 調整 ¥0 ＝ 売上 ¥17,189
       | 配達時間 6:41:58 | 走行 62.02km | MAP 16/16（18:09 は撮り直し 205021.png から切り抜き）| 経費: アプリ入力分（HANDOFF未把握）
       trip ID: del_0924_1〜16（17:39 = del_0924_14、18:09 = del_0924_15 は別Delivery）/ quest_0924_1（80回・special・達成19:15/計上19:23）/ quest_0924_2（6回・normal）。15:00 の 6回乗車クエスト ¥0 は非加算。
     - 今週（9/21〜）: 売上 ¥41,405 / 80件（80回乗車クエスト達成と一致）。今月: 売上 ¥88,630 / 152件 / 125trip（アプリ同梱データ分）。
  6. テスト（node tools/official-import/import.js test: 216項目 全PASS）:
     - 4-2（修正C書の正解値）: 16件・¥7,699・17:39/18:09 別Delivery・¥320 の7件非統合・重複スクショ1枚化・¥600/¥8,890 各1回・¥9,490・特別クエスト識別・¥17,189・
       アプリ計算（総売上・特別クエスト内訳）・再取込で変更なし・中身が違う同時刻同額スクショは停止・07:42 スクショ不足は停止。
     - 4-3（統合ルール）: 60分超は統合しない・金額違いは統合しない・計上が達成より前は統合しない・達成表示のみは1件計上・特別判定・1対1。
     - 既存: 9/21・9/22・9/23 の再取込は変更なし（二重登録なし）、既存の同時刻クエスト統合・¥0 クエスト・既存日の表示と集計は従来どおり。
     - テスト基盤: 端末同期テスト（9節）の端末データを一時ファイル渡しに変更（データ増加で Windows のコマンドライン長上限を超えたため）。
  7. 変更ファイル: tools/official-import/lib/activity-parser.js・lib/pipeline.js・README_JA.md・spec/run-tests.js・spec/fixtures/2026-09-24.*（新規）/
     js/store.js（9/24 データ・特別クエスト集計）・js/trip-maps.js・assets/maps（9/24 の16件）・js/ui.js・css/app.css / version: 20260924_v33（apply の自動更新）

■ v32（2026-09-25）: バイクシェア自動入力額の修正・v27 実機確認済み
  - 経費入力で「バイクシェア」を選んだ時の自動入力を 内容「バイクシェア1日パス」・金額「527」に修正（v31 の 1527 は誤り）。手動変更可能・保存ロジックは変更なし。
  - v27 の「スマホで登録した経費が後から消える」修正は、ユーザーがスマホ実機で問題ないことを確認済み（2026-09-25）。
  - 変更ファイル: js/ui.js（自動入力額）/ version: 20260925_v32。その他の v31 の変更は変更なし。

■ v31（2026-09-25）: 履歴カレンダーの表示改善・日曜／祝日の識別・経費入力の初期値
  1. カレンダー（表示のみ。実データ・集計・他画面の金額表示は変更なし）:
     - 説明文をカレンダーのカードの外へ分離し、文言を「日付をタップすると日別評価が見れます。『休』は非稼働日。金額は100円未満を切り捨てて表示しています。」に。
       文字サイズは「2026年9月」（.cal-title）と同じ 16px。
     - 稼働日の金額はカレンダー上だけ100円未満切り捨て・カンマなし（例: ¥9,243 → ¥9200 / ¥10,683 → ¥10600 / ¥21,310 → ¥21300）。「¥」は表示。
     - 日曜日と日本の祝日（振替休日・国民の休日を含む）の日付を赤系で表示（祝日名は title に表示）。土曜は祝日でなければ色なし。曜日見出しの「日」も赤系。
       2026年9月: 日曜 6・13・20・27、祝日 21 敬老の日・22 国民の休日・23 秋分の日。
  2. 日本の祝日の判定（js/store.js）:
     - 以前は 2026-09-21〜23 だけの固定表（JAPAN_HOLIDAYS）だったため、computeJapanHolidays(年) / getJapanHolidayName(日付) で年ごとに計算する方式へ変更。
       固定日・ハッピーマンデー・春分／秋分（1980〜2099年の近似式）・振替休日・国民の休日に対応（現行法の規定。過去の特例年は非対応）。
     - JAPAN_HOLIDAYS は後方互換のため計算結果を展開した同じ形で残した（getDayOfWeekInfo・次回振込日の営業日判定も計算方式を使用）。
       ※ 振込予定日の営業日判定は、今後の祝日（10月以降など）も正しく考慮されるようになった。
  3. 経費入力（稼働画面）:
     - 種類の並びを 1.必要経費 2.バイクシェア とし、初期値を「必要経費」（内容・金額は空欄。入力欄の例文も「内容を入力」「金額を入力」に変更）。
     - 「バイクシェア」を選ぶと 内容「バイクシェア1日パス」・金額を自動入力（手動で変更可能）。※ v31 では 1527 としたが、v32 で正しい 527 に修正。
     - 「必要経費」へ戻すと内容・金額を空欄に戻す。登録後は種類「必要経費」・内容と金額空欄に戻る。経費の保存ロジックは変更なし。
  4. 変更ファイル: js/store.js（祝日計算）/ js/ui.js（カレンダー表示・経費入力の初期値）/ index.html（経費の選択肢順・例文）/ css/app.css（カード分離・説明文・赤系日付）/
     tools/official-import/spec/run-tests.js（10-6 祝日）/ version: 20260925_v31
  5. 確認: 画面の実操作（経費: 初期値・バイクシェア自動入力・手動変更・必要経費で空欄 / カレンダー: 金額表記・赤系日付・祝日名・説明文の位置と16px）、テスト 195項目 全PASS。

■ v30（2026-09-25）: 履歴カレンダーの説明文と一覧の非稼働日
  - カレンダー下部の説明文を「日付をタップすると日別評価が見れます。『休』は非稼働日。」だけに変更（未入力日などの補足は削除）。文字サイズ 11px → 22px（約2倍・自然に折り返し）。
  - 一覧表示から非稼働が確認済みの日（CONFIRMED_DAY_OFF_DATES: 9/12・9/13・9/20）を除外。スマホ端末に残る 9/20 の日別データ自体は削除・変更していない。
    9/12・9/13・9/20 はカレンダーでは「休」、一覧には出さない。配達実績・売上のある日は常に一覧に出る（getDayStatus が worked のため除外対象外）。
  - 変更ファイル: js/ui.js（説明文・一覧の除外条件）/ css/app.css（.cal-note）/ version: 20260925_v30。計算ロジック・その他のUIは変更なし。
  - 確認: 端末に 9/20 の稼働開始のみのデータがある状態で、一覧は 9/23〜9/10 の稼働日11日のみ（9/20 なし）、カレンダーは 9/12・9/13・9/20 が「休」、説明文が大きく折り返し表示。テスト 190項目 全PASS。

■ v29（2026-09-25 実装）: 日別履歴の非稼働日表示の整理＋カレンダー表示

1. 稼働状況（2026年9月・ユーザー確認済み）:
  - 9/10 稼働日 / 9/12・9/13・9/20 非稼働日。
  - 9/12・9/13 は日別データが無く一覧にも出ない（現状どおり。一覧へは追加しない）。
  - 9/20 は同梱データ（CONFIRMED_SEED_DATA）には無く、スマホ端末側の日別データ（稼働開始時刻・手動記録など、配達実績・売上の無いデータ）により一覧に表示されている。
    データは削除・変更せずそのまま尊重する。

2. 「利益 --」の非表示:
  - 日別履歴の日付バーで、売上が無く利益を計算できない日（netProfit が null）は利益欄そのものを表示しない（以前は「利益 --」）。
  - 売上・利益・件数の計算は変更なし。

3. 一覧／カレンダー切替（履歴タブ）:
  - 見出し右に［一覧］［カレンダー］。選択は端末に記憶（localStorage: uber_log_history_view）。一覧は従来どおり（売上・利益・件数・B/調/賞/♥・日別詳細）。
  - 見出しの期間は一覧に並ぶ日の最初〜最後から自動表示（例: 26/9/10～9/23。以前は「26/9/10～9/19」固定だった）。
  - カレンダー（月単位・日曜始まり・前月／次月）:
      稼働日 = 配達実績（件数）または売上がある日 → 緑の枠で「件数」＋小さく売上。タップで一覧に切り替え、同じ日別詳細を展開。
      休 = 稼働していないことが確認済みの日（store.js の CONFIRMED_DAY_OFF_DATES: 2026-09-12 / 09-13 / 09-20、または日別データの dayOff: true）→ グレーで「休」。
      それ以外（データの無い日・未来・判定できない日）→ 日付のみ（空欄）。「データが無い＝休み」とは判定しない。
      今日は枠線で強調。月の「稼働 N日・休み N日」を表示。
  - 一覧とカレンダーは同じ日別データ・同じ計算（store.getDayStatus は getCalculatedMetrics を使用）を参照。B/調/賞/♥ はカレンダーには出さない（一覧で確認）。
  - 今後の非稼働日を「休」にするには CONFIRMED_DAY_OFF_DATES に日付を追加する（画面からの登録機能は未実装）。

4. 変更ファイル: js/store.js（CONFIRMED_DAY_OFF_DATES・getDayStatus）/ js/ui.js（利益欄の非表示・表示切替・カレンダー・見出し期間）/
   index.html（表示切替ボタン・カレンダー枠）/ css/app.css（切替・カレンダーのスタイル）/ tools/official-import/spec/run-tests.js / version: 20260924_v29

5. テスト: node tools/official-import/import.js test 190項目 全PASS（10-5: 「休」は確認済みの3日だけ、稼働日の件数・売上は一覧と同じ、データなし・未来は空欄、
   端末に 9/20 の日別データがある状態でも「休」・利益なし）。画面確認（ローカル表示・端末に 9/20 の稼働開始のみのデータを置いた状態）:
   カレンダー 稼働11日・休み3日（12・13・20 が休、1〜9・25〜30 は空欄）、一覧の 9/20 は利益欄なし、カレンダーの 9/22 タップで一覧の 9/22 詳細が開く。

■ v28（2026-09-24 実装）: チップを独立項目として表示・集計（♥）

1. データの扱い（二重計上なし）:
  - 各配達: fee = Uber公式の最終売上（チップ込み）。チップがある配達は baseFee（基本料金）と tip（チップ）を内訳として保持（fee = baseFee + tip）。
  - 日別: getCalculatedMetrics に tipSales（その日の tip 合計）・tipCount・baseDeliverySales（配達報酬からチップを除いた額）を追加。
    チップは deliverySales（配達報酬＝最終売上の合計）に既に含まれているため、totalSales・netProfit には別途加算しない（総売上・利益は従来と同じ値）。
  - 週次: getRevenueSummary().thisWeek に baseDeliverySales / tipSales / adjustmentOnlySales / otherOnlySales を追加（既存の deliverySales・otherSales 等は意味を変えずに維持）。
  - 期間指定: store.getSalesBreakdown(開始日, 終了日) を新設（配達報酬（チップ除く）/ チップ・件数 / クエスト / 特別ボーナス / 調整 / その他 / 総売上 / Bike）。
    配達報酬（チップ除く）＋チップ＋クエスト＋特別ボーナス＋調整＋その他 ＝ 総売上。

2. 既存データの確認結果（データの書き換えはなし）:
  - 2026-09-22 No.6 del_0922_6 マクドナルド 九条店 10:50: 基本料金 ¥320 ＋ チップ ¥50 ＝ 最終売上 ¥370（v21 で登録済み: baseFee 320 / tip 50 / fee 370）。
  - 2026-09-23 No.17 del_0923_17 ローソン 靱本町三丁目 14:53: 基本料金 ¥320 ＋ チップ ¥218 ＝ 最終売上 ¥538。
    公式 Delivery 詳細スクショ（「¥218 のチップを含む」／売り上げ欄 料金 ¥320・チップ ¥218、撮り直し画像 2026-09-23_1453.png）の読取値として v24 の公式取込で登録済み（baseFee 320 / tip 218 / fee 538）。
  - 9/23 のチップがある配達は No.17 の1件のみ（他の19件の公式スクショにチップ表示なし）。推測での入力・変更は行っていない。
  - 2026年9月のチップ累計: ¥268（2件）。

3. 表示:
  - 履歴の日付バー: チップがある日に「♥」（B・調・賞と同じ24px四角、ローズ系の色）。並び順 B → 調 → 賞 → ♥。チップ0円の日は表示なし。
  - 日別詳細: 追加収支カードに「♥ チップ ¥xxx（配達報酬に含む・N件）」（総売上へ加算しないため「+」は付けない）。
  - 配達明細: チップがある配達に「♥ チップ¥xxx」を小さく表示。
  - 分析 → 今週の売上利益の内訳を 2列×4段（配達報酬（チップ除く）/ チップ / クエスト / 特別ボーナス / 調整 / その他 / Bike / 配達件数）に。
    調整を「その他」から独立。各項目の合計＝売上（今週 9/21〜: ¥20,505 ＋ ¥268 ＋ ¥4,050 ＋ ¥0 − ¥607 ＋ ¥0 ＝ ¥24,216）。
  - 分析 →「売上内訳（期間指定）」カードを追加（開始日〜終了日、初期値は今月1日〜今日）。チップ累計と件数を表示。
  - スマホPWAの4タブ構成・今月の実績カードは変更なし。

4. 変更ファイル: js/store.js（チップ集計・♥属性・週次内訳・getSalesBreakdown）/ js/ui.js（♥表示・チップカード・週次内訳・期間指定カード）/
   index.html（週次内訳8項目・期間指定カード）/ css/app.css（♥・チップ・期間指定カードのスタイル）/ tools/official-import/spec/run-tests.js（チップのテスト）/ version: 20260924_v28
   ※ index.html の css/js 読込のキャッシュ回避クエリ（?v=）が 20260920_v16/v19 のまま古かったため 20260924_v28 に更新（以後は公式取込 apply のバージョン自動更新で一緒に更新される）。

5. テスト: node tools/official-import/import.js test 185項目 全PASS（10-4 チップ: 9/22・9/23 のチップ額と総売上・利益の不変、♥はチップのある日だけ・並び順、
   全日で「配達報酬（チップ除く）＋チップ＝配達報酬」、今週内訳の合計＝売上、期間指定の内訳合計＝総売上・チップ累計）。
   画面確認（ローカル表示）: 日付バー 9/23 ♥ / 9/22 B調♥ / 9/21 B / 9/19 B賞 / 9/18 B調、9/22 詳細の 調・♥チップ¥50・B カード、No.6・No.17 の ♥ 表示、分析の内訳・期間指定カード。

■ v27（2026-09-24 緊急修正）: スマホで登録した経費が後から消える問題（修正完了・スマホ実機確認済み 2026-09-25）

【症状】2026-09-23（公式取込済みの日）にスマホ（GitHub Pages/PWA）で Bike 経費を登録 → 直後は利益低下・B表示・明細あり → 時間を置くと経費明細そのものが消え、利益が元に戻り B も消えた。

【原因（本物の store.js / cloud-sync.js を端末ごとに独立実行する再現テストで特定・旧コードで再現を確認）】
  原因1: 端末保存の丸ごと上書き（同じ端末）
    - store.saveToStorage() が「その画面のメモリ上の全データ」を LocalStorage へそのまま書いていた。
    - 同じ端末で別の画面（別タブ・ブラウザとホーム画面アプリ・経費登録前に開いていた古いインスタンス）が1つでもあると、
      その画面で何か保存（○×評価など）した瞬間に、経費を知らない古いメモリ内容で保存データ全体が上書きされ、登録済み経費が消える。
    - 消えた状態でその日が送信されると、クラウドの経費も消える。
    - 経費は日別ログの中だけに保存されている一方、○×評価・MAPメモは別キーからも復元されるため、症状は「経費だけが消える」形で現れる。
  原因2: クラウドへの丸ごと上書き（端末間）
    - cloudSync.pushPendingChangesInternal() が送信前にクラウドと統合せず、端末の日別ログ全体を upsert していた。
    - 9/23 の経費を知らない端末（例: PC の localhost）が 9/23 の何か（○×評価など）を保存すると、クラウドの 9/23 から経費が消え、
      クラウド経由で読む端末（別ブラウザ・別のホーム画面アプリ・新しい端末）では経費が無い状態になる。
  関連して修正した不具合:
    - 送信中にその日を変更すると、未送信フラグが消えて変更が送られないことがあった（送信後に無条件で pending を削除）。
    - 同じ経費IDの統合がローカル優先固定で、updatedAt の新しい修正が負けることがあった。
    - 9/18・9/19 の公式シード補完が「経費の先頭が バイクシェア利用 でなければ経費配列を丸ごとシードで置換」しており、ユーザーが追加した経費を消したり、削除済みの確定Bike経費を復活させたりしていた。
  ※ pull 時の統合（mergeDailyLog の経費ID和集合）と officialImport 日の同期（syncOfficialImportedDay）は経費に触れておらず、原因ではなかった。

【修正内容】
  - js/store.js: 経費のID単位マージを共通化（mergeExpenseLists / mergeDayUserData）。
      片方にだけある経費は必ず残す／同じIDは updatedAt（無ければ createdAt）が新しい方／deletedExpenseIds は和集合で削除済みは復活させない。
      workSessions・manualTapsArchive も ID 単位で統合。
  - js/store.js: 端末保存 persistState() を新設。保存のたびに LocalStorage の最新内容を読み直し、日ごとに統合してから書く（古い画面の上書きで消えない）。
      saveToStorage はこれを使う。全データの復元（importJSON）・全削除（clearAllData）のみ統合せずにそのまま書く。
  - js/store.js: 別の画面が保存したら（storage イベント）メモリ内容を読み直して再描画。
  - js/store.js: 9/18・9/19 のシード補完は「Bike経費が1件も無く、かつ削除済みでない場合だけ確定Bike経費を追加」に変更（既存経費は置換しない）。
  - js/cloud-sync.js: 送信前にクラウドの現在の行を取得し、日ごとに mergeDailyLog で統合してから送信（統合結果は端末にも保存）。
  - js/cloud-sync.js: pull 後の端末保存も persistState()（統合書込）に変更。経費の統合は共通の mergeExpenseLists（updatedAt 比較）を使用。
  - js/cloud-sync.js: 日付ごとの変更回数を記録し、送信中に変更された日は pending を残して次回送信。
  - officialImport で更新するのは trip・sales・quests・adjustments・公式距離/時間・MAP のみ（従来どおり）。expenses・deletedExpenseIds・workSessions・○×・理由・MAPメモ・manualTapsArchive は同期・公式シード補完で消さない。
  - 9/23 の消えた経費はコードに固定追加していない（ユーザーがスマホから改めて1回登録して実機確認する）。

【テスト（node tools/official-import/import.js test: 177項目 全PASS。うち spec/sync-tests.js 27項目を新設）】
  - spec/sync-harness.js: 本物の store.js・cloud-sync.js を端末（タブ）ごとに独立した実行環境で動かし、メモリ上のクラウド表を共有する再現用ハーネス。
  - 実機シナリオ: 9/23 公式取込済み → スマホで Bike ¥1,527 追加（利益 ¥9,156・B 表示）→ クラウドは経費の無い古い状態 → pull・統合で維持 → 再読込で維持
    → 送信 → 別端末 pull で同じ経費が1件だけ（二重登録なし）→ 公式シード再同期（公式データへ戻る）後も維持。
  - 原因1・2の再現と防止、削除 tombstone（古い端末が持っていても復活しない・全削除で B 消去/利益復帰）、修正の updatedAt 採用、
    送信中の変更の再送、9/18 追加経費の非置換・削除済み確定Bike経費の非復活、全削除は統合しない。
  - 修正前のコード（HEAD の store.js・cloud-sync.js）に同じテストを当てると 11項目 FAIL（原因1・2を含む）→ 修正後 27項目 PASS を確認。
  - 回帰: 9/23 公式 20trip / 23配達 / ¥10,683 / MAP 20/20、9/22 Support Adjustment -¥607、既存Bike経費、B/調/賞、経費削除 tombstone、公式取込 v26（取り込み実行・localhost）すべて既存テストで PASS。
  - バージョン: 20260924_v27（index.html / sw.js / version.json）。スマホは更新検知で自動再読込される。

【実機確認（2026-09-25 ユーザーがスマホ実機で問題ないことを確認済み）】
  スマホで v27 に更新後、9/23 の Bike 経費を改めて1回だけ登録 → 時間を置いて・アプリ再起動後・PC で UBER_LOG を開いた後も、経費・B・利益が維持されることを確認する。
  ※ 同じ端末に古い UBER_LOG の画面（別タブ等）が開いている場合は、一度閉じてから確認するとより確実（修正後は古い画面が保存しても消えない）。

■ v26（2026-09-24 実装）: 毎日の公式取込を1画面・1ボタンに統合

【毎日の操作（ユーザー）】※これ以外は不要（PowerShell・python -m http.server・UBER取込.cmd・Claude Code 手動起動・/uber-import 入力はすべて不要）
  ① デスクトップの「UBER_LOG」をダブルクリック → ② 日付を確認 → ③ Uber一覧を貼る → ④ スクショを全部ドラッグ → ⑤「取り込み実行」→ 完了結果を見る（「UBER_LOGを見る」で本体へ）

1. 起動方式:
  - デスクトップの「UBER_LOG.lnk」（tools/official-import/install-shortcut.ps1 で作成済み・再作成可）→ wscript.exe tools\official-import\launcher.vbs（コンソール画面なし）。
  - launcher.vbs: http://127.0.0.1:8088/api/health で UBER_LOG backend が起動済みか確認 → 未起動なら node tools\official-import\server.js を非表示で起動（起動済みなら二重起動しない）
    → http://localhost:8088/tools/official-import/ を既定のブラウザで開く。
    ポート8088を別のプログラム（以前の手動 python -m http.server 等）が使っている場合は起動せずメッセージ表示。Node.js が無い場合もメッセージ表示。
  - 以前ユーザーが手動で起動していた python -m http.server 8088（全インターフェースで待受け＝LANにも公開されていた）はユーザー了承のうえ停止済み。今後は不要。

2. ローカルbackend（tools/official-import/server.js・Node.js 標準機能のみ・外部ライブラリなし）:
  - Node.js を選択（既存の取込パイプラインが Node 製のため同一ランタイムで最も安全）。
  - 待受けは 127.0.0.1 と ::1 のみ（LAN・外部に非公開。テストで LAN アドレスから接続不可を確認）。Host ヘッダーが localhost/127.0.0.1 以外は拒否（DNS rebinding 対策）。
  - API は独自ヘッダー X-UBER-LOG: 1 必須＋他サイト Origin 拒否（他サイトのページから取込を実行させない）。
  - 配信: / = 通常の UBER_LOG（リポジトリをそのまま配信）、/tools/official-import/ = 公式取込画面。inbox・staging・logs・ドットファイル（.git 等）は配信しない。
  - API: health（起動確認）/ inbox/<日付>（保存済みの一覧・スクショ名）/ import（取り込み実行・POST）/ jobs/<id>（処理状況）。いずれも /api 配下。
  - ログ: tools/official-import/logs/server.log（gitignore）。
  - sw.js: /api 配下をキャッシュ対象外に（GitHub Pages 公開版には /api が無いため影響なし）。

3. 取込実行フロー（tools/official-import/lib/import-job.js。判断ロジックは既存 pipeline.js をそのまま使用）:
  0) 取込で更新するファイル（js/store.js・js/trip-maps.js・assets/maps・index.html・sw.js・version.json・HANDOFF.md）に未コミット変更があれば混ぜずに停止
  1) 保存: inbox/<日付>/activity.txt・screenshots/（画面で × にした画像は削除せず inbox/<日付>/_previous/<時刻>/ へ退避）・decisions.json（画面の「MAPなしで取り込む」）
  2) 一覧解析: 既存 prepare（lib/run-step.js 経由で子プロセス実行）
  3) スクショ確認: 未読・内容が変わった画像（sha256 で判定）だけ Claude Code を非対話で起動して読取（lib/screen-reader.js）
     起動: claude.exe -p --tools Read --permission-prompts none --output-format json --json-schema <スキーマ> --no-session-persistence（8枚ずつ）
     → 使えるのは Read のみ・許可が要る操作は自動拒否・権限スキップ不使用。構造化出力（structured_output）を backend が staging/<日付>.screens.json へ書く。
     読取ルールは /uber-import と同じ（推測禁止・読めない値は UNKNOWN・配達先は丁目まで・ローマ字のみの配達先は区名を推測せず原文を note）。
  4) MAP作成・検証: 既存 validate（MAP crop 含む）→ FAIL なら本体に書かず「確認が必要です」
  5) UBER_LOG反映: 既存 apply（取込済みで差分なしなら「完了（取込済み・変更なし）」で終了・二重登録なし）
  6) テスト: node tools/official-import/import.js test（FAIL なら commit しない）
  7) 記録: 既存 report → HANDOFF の「公式取込 自動記録」ブロック（開始／終了マーカー間・日別行＋最新集計）を自動更新（lib/handoff.js、手書き部分は変更しない）
     → HANDOFF.md へ同期 → 取込対象ファイルのみ git add → commit「data(<日付>): official import (<N> trips, ¥<総売上>)」→ push
  8) 完了画面: 日付・トリップ・配達・配達報酬・クエスト・調整・総売上・距離・配達時間・MAP・VALIDATION PASS・［UBER_LOGを見る］（http://localhost:8088/）

4. エラー・確認時の動作（すべて取込画面の中に表示。PowerShell には戻さない）:
  - 処理中は「保存中 → 一覧解析中 → スクショ確認中 → MAP作成・検証中 → UBER_LOG反映中 → テスト中 → 記録・保存中 → 完了」を表示。
  - MAP不足: 「N件のMAP画像を確認できません」＋時刻・金額・ファイル名、［スクショを追加する］［MAPなしで取り込む］。
  - 読めない項目・Delivery詳細以外の画像・件数や金額の不一致・未分類イベント等: 項目ごとに表示（本体は非反映）。
  - Claude Code が無い: 「Claude Code が見つかりません」を表示して停止。テスト失敗: 反映済み・未 commit であることを表示。push 失敗: commit 済みであることを表示。
  - 同時実行は1件のみ（実行中は受付しない）。未来日は受け付けない。

5. 旧入口の扱い:
  - v25 の UBER取込.cmd はユーザー確認のうえ削除（リポジトリからも削除）。
  - 保守用として残す: run-latest-import.ps1（最新日付で対話型 /uber-import を開始）、Claude Code の /uber-import YYYY-MM-DD、import.js の各コマンド。README は保守用節に移動。

6. 既存PWA: スマホの4タブ（稼働・履歴・分析・地雷）・画面は変更なし。GitHub Pages 公開版はローカルAPIに依存しない（取込画面は backend が無いと「PCのローカル専用」表示のみ）。
   ※ localhost の UBER_LOG は GitHub Pages とは別オリジンのため、端末保存データ（経費等）はクラウド同期ログイン時のみ共通。

7. テスト（node tools/official-import/import.js test: 150項目 全PASS。うち spec/server-tests.js 32項目＋ランチャー4項目）:
  - backend: 配信（/・/tools/official-import/）・Host/ヘッダー/Origin 拒否・生データ非配信・LAN から接続不可・未来日拒否
  - 取り込み実行（一時リポジトリ＋送信先 bare リポジトリで実行）: 保存（日付フォルダ・activity・スクショ）→ MAP不足で「確認が必要です」（14:48 ¥331 を表示・本体非反映）
    → MAPなしで取り込む → 反映・HANDOFF 自動記録・HANDOFF.md 同期・commit（取込対象ファイルのみ）・push → git status clean
    → 同日再実行で「変更なし」・commit なし / 読めないスクショ・件数不一致で本体非反映 / 未コミット変更があれば停止 / × の画像は _previous へ退避 / Claude Code なしで停止
  - ランチャー: 未起動→非表示で自動起動 / 起動済み→二重起動しない（同一pid）/ 待受けはループバックのみ / 別プログラムがポート使用中→起動しない
  - 実環境確認（2026-09-24）: launcher で 8088 に起動（127.0.0.1・::1 のみ）→ 画面と同じ API で 2026-09-23 を取り込み実行 → 「完了（取込済み・変更なし）」
    20trip / 23配達 / ¥10,683 / MAP 20/20、HEAD・js/store.js 不変。取込画面は保存済みの一覧・スクショ20枚を自動読込し、処理ステップの進行表示を確認。
  - スクショ読取（claude -p・Read のみ・構造化出力）は実際の 9/23 スクショ1枚で動作確認済み（13:14 ¥320 を正しく返却）。
  - バージョン: 20260924_v26（index.html / sw.js / version.json）

■ v25（2026-09-24 実装）: 履歴Bバッジ修正＋毎日取込ワンクリック化（UBER取込.cmd）

【毎日の操作（v25時点・v26で置き換え済み）】
  ① Uberアクティビティ一覧をコピー → ② 取込画面 tools/official-import/index.html へ貼付 → ③ Delivery詳細スクショを全部ドラッグ
  → ④［取込データを保存］ → ⑤ UBER_LOG\UBER取込.cmd をダブルクリック

A. 履歴の日別「B」バッジ修正
  - 原因: getDayAttributes() のB判定が経費カテゴリの完全一致（「バイクシェア利用」「レンタバイク」「レンタサイクル」）＋ 9/18・9/19 の日付固定だった。
    v20 の稼働画面の経費入力はカテゴリ「バイクシェア」で保存するため一致せず、9/23 にアプリから登録したBike経費で B が付かなかった。
    一方、週次・月次のBike集計は「バイク／サイクル／bike を含む」判定だったため、利益には反映されていた（判定ロジックの二重化が原因）。
  - 修正: js/store.js に共通判定 isBikeExpenseCategory(category) / isBikeExpense(expense) を新設（カテゴリ未設定・「バイク」「サイクル」「bike」を含む＝Bike、amount>0）。
    履歴Bバッジ・週次Bike・先週Bike・月次Bike・9/21-22のBike補完判定・経費一覧の種類表示（js/ui.js）をすべてこの共通判定に統一。日付固定の B 付与は廃止。
  - log.expenses[] との連動: その日の経費にBike経費が1件でもあれば B。必要経費のみの日は B なし。Bike経費を全件削除すると B も消える。
    履歴タブは表示のたびに store の現在データから再描画されるため、経費の登録・修正・削除後に履歴へ戻った時点で B が更新される（再読み込み不要）。
  - 既存の B（9/18・9/19・9/21・9/22）は各日の確定Bike経費データで維持。「調」「賞」の判定は変更なし。公式trip・MAP・売上・クエスト・Adjustment は変更なし。
  - 確認: 自動テスト（登録→B / 必要経費へ修正→消える / 戻す→B / 全削除→消える / 必要経費のみ→なし / 再読込後→B / 既存B・調・賞維持）、
    実画面確認（9/23 にバイクシェア経費を入れた状態で履歴タブ表示 → 9/23 に B、9/22 B調 / 9/21 B / 9/19 B賞 / 9/18 B調）。

B. UBER取込.cmd（毎日の /uber-import を手入力しない入口）
  - ファイル: UBER_LOG\UBER取込.cmd（ASCIIのみ・PowerShellスクリプトを呼ぶだけ）→ tools\official-import\run-latest-import.ps1（UTF-8 BOM付き）
  - 最新日付の自動判定: inbox\ 直下の YYYY-MM-DD 形式フォルダのみ対象。DateTime として解析できるもの（2026-13-01 等は無視）を日付で比較し最新を選択。
    今日より未来の日付フォルダは自動選択せず黄色で警告。日付フォルダが無ければエラー。
  - 実行前チェック（1つでも不足なら Claude Code を起動しない）: activity.txt がある・空でない / screenshots フォルダがある / PNG・JPG が1枚以上。
  - Claude Code の起動方式（このPCの Claude Code 2.1.280 の `claude --help` で確認）:
    `claude [prompt]` は "starts an interactive session by default"（-p/--print は非対話）。これを使い、UBER_LOG 直下で
    `claude "/uber-import <日付>"` を実行 → 対話セッションが開き、最初の指示として /uber-import <日付> が自動投入される。
    取込中の質問（MAP不足・スクショ不一致・重複候補・UNKNOWN 等）や許可確認にも同じ画面で回答できる。
    --dangerously-skip-permissions 等の権限スキップは使っていない（許可確認は通常どおり表示される）。
  - エラー時の挙動: 「UBER_LOG 公式取込 / 対象: <日付> / エラー: <理由>」を日本語で表示し、キー入力までウィンドウを閉じない。
    claude が PATH に無い場合は「Claude Codeが見つかりません」と表示して停止（インストールはしない）。
    Claude Code 終了後（/exit）も「終了しました（対象日・終了コード）」を表示してキー入力待ち。
  - 取込処理本体（prepare / validate / apply / test / report / HANDOFF / commit / push）は既存 /uber-import のまま変更なし。
  - 別の日を取り込み直すときは、従来どおり Claude Code で /uber-import YYYY-MM-DD を直接実行する。
  - 制約: UBER取込.cmd は Claude Code の対話画面を開くため、取込の完了確認は画面上の Claude Code の報告を見る（自動で閉じない）。

C. テスト・確認結果（node tools/official-import/import.js test: 114項目 全PASS）
  - 取込cmd: 最新日付選択 / 無効フォルダ無視 / 未来日警告 / activity.txt なし→停止 / screenshots なし・0枚→停止 / Claude Code なし→停止 /
    正常時は UBER_LOG 直下で「/uber-import 2026-09-23」を引数に起動（偽の claude で受取引数と作業フォルダを確認）/ 終了後の案内表示。
  - 実データ: run-latest-import.ps1 -DryRun で実際の inbox から 2026-09-23 を自動選択・スクショ20枚を確認。
  - 9/23 再取込（/uber-import と同じ prepare → apply）: VALIDATION PASS・「既に取込済みで変更はありません（二重登録なし）」。
    js/store.js・js/trip-maps.js・version.json はバイト単位で不変、assets/maps のファイル数不変。9/23 の 20trip / 23件 / ¥10,683 / MAP 20/20 を維持。
  - 実機確認: ユーザーが UBER取込.cmd をダブルクリックし、Claude Code の対話画面が開いて /uber-import 2026-09-23 が自動で開始されることを確認済み（2026-09-24）。
  - 取込ツールの堅牢化: pipeline.js の store.js 読取を「シード末尾の括弧対応」で判定するよう変更（シード直後のコード配置に依存しない）。
  - バージョン: 20260924_v25（index.html / sw.js / version.json）
■ v23 公式取込 v1（2026-09-23 実装）: 毎日の公式データ取込の仕組み

【毎日の操作（ユーザー）】
  ① Uberアクティビティ一覧をコピー → ② 取込画面 tools/official-import/index.html（Chrome/Edge）へ貼付
  → ③ Delivery詳細スクショを全部ドラッグ → ④［取込データを保存］ → ⑤ Claude Code で /uber-import YYYY-MM-DD
  説明書: tools/official-import/README_JA.md

1. 設計（パイプライン）:
   inbox/<日付>/activity.txt ＋ inbox/<日付>/screenshots/*（生データ）
     → prepare: 一覧解析＋スクショ読取テンプレート staging/<日付>.screens.json 作成
     → Claude Code が各スクショを Read で実際に見て記入（読めない項目は "UNKNOWN" のまま → 検証で停止）
     → validate: 照合・MAP切り抜き・検証 → staging/<日付>.json（正規化データ＋検証結果）
     → apply: 検証PASS時のみ既存方式で js/store.js（CONFIRMED_SEED_DATA の対象日ブロックのみ差替/挿入）・js/trip-maps.js・assets/maps へ反映、バージョン自動更新
     → test → report（日次・週次・月次・クエスト進捗）→ HANDOFF → commit/push
   コマンド: node tools/official-import/import.js prepare|validate|apply|report <日付> / test

2. フォルダ構成（tools/official-import/）:
   README_JA.md / index.html（PC用取込補助画面）/ import.js（CLI）
   lib/activity-parser.js（一覧解析・クエスト重複排除。画面とCLIで共用）/ lib/pipeline.js（照合・検証・反映）/ lib/crop_map.py（MAP切り抜き・Pillow）
   spec/run-tests.js ＋ spec/fixtures（9/21・9/22確定データの一覧テキスト・スクショ読取値）
   inbox/<日付>/{activity.txt, screenshots/, decisions.json, manifest.json}（Git管理外）
   staging/（Git管理外）

3. .gitignore: tools/official-import/inbox/ ・ tools/official-import/staging/ ・ __pycache__/ ・ .claude/settings.local.json
   ※生スクショ・生activity・中間データ（住所等を含み得る）はコミットしない。反映後の assets/maps/full_*.png・map_*.png は従来ルールどおりコミット対象（変更なし）。

4. 取込補助画面（tools/official-import/index.html）:
   - 日付・一覧textarea・スクショのドラッグ＆ドロップ・画像枚数・一覧の簡易解析（Delivery/クエスト行/調整件数と金額、未分類・重複候補・他日付・日付不明の注意）
   - File System Access API で inbox フォルダへ直接保存（初回だけフォルダ選択、ハンドルは IndexedDB に記憶）。非対応ブラウザは activity.txt ダウンロード＋手動配置の案内。
   - 幅800px超の画像（画面全体スクショ）は警告。スマホPWAの下部4タブ（稼働/履歴/分析/地雷）には追加していない。

5. /uber-import コマンド: .claude/commands/uber-import.md（引数 YYYY-MM-DD）。手順・禁止事項・報告形式を記載。

6. activity解析ルール（lib/activity-parser.js）:
   - 種別行（Delivery / クエスト / QUEST / MISC / 調整 / Support Adjustment 等）＋日付＋時刻＋金額＋View Details＋URL を1イベントとして解析。タブ区切り1行形式も可。
   - 日付: 英語（Tuesday, September 22nd, 2026 / Sep 22）・日本語（2026年9月22日 / 9月22日）・ISO。日付の無い行は直前の日付を引継ぎ。取込日以外は対象外。
   - 時刻: 19:48 / 7:48 PM / 午後12時53分。金額: ￥483 / -￥607 / ￥-607 / −¥607（符号付き）。
   - URL の /trips/<UUID> → trip UUID、その他のUUID → activity UUID。同一UUIDの重複行は1件に統合。
   - 「合計」行は総売上の照合に使用。特別報酬（保証・プロモーション等）・チップ単独・未分類は自動取込せず停止。

7. クエスト重複ルール: 同時刻・同額で「クエスト（MISC）」と「N回乗車クエスト（QUEST）」の2行 → 同一報酬として1回だけ計上（計上分に note で記録）。¥0クエストは記録のみで売上非加算。
   それ以外の同時刻・同額の重なりは推測で消さず「重複候補」で停止 → inbox/<日付>/decisions.json の questDuplicates（"HH:MM|金額": count_once / count_all）で確認後に取込。
   取込後にアプリの deduplicateQuests がクエストを誤って除外しないことも検証。

8. Adjustment ルール: Uberの調整はプラス／マイナスとも売上の「その他」（dailyLogs[date].adjustments[] ＋ sales.adjustment）。経費ではない。一覧上の公式名称（Support Adjustment 等）を officialTitle に保持。

9. スクショ照合: 一覧の Delivery とスクショは「日付・時刻・金額」が完全一致したものだけを同一tripとする（trip UUID があれば一致も確認）。近いtripへの寄せ・並び順だけでの紐付けはしない。
   同時刻・同額が複数ある場合のみ件数一致を条件にファイル名順で対応。配達先は丁目まで（先頭の都道府県コード27等を除去・全角数字を半角化）、番地・部屋番号等があれば停止。

10. validation（1つでも×なら本体へ反映せず停止・ズレた項目のみ報告）:
   Delivery件数（一覧＝スクショ＝照合済み）/ 配達報酬合計（一覧＝trip合計）/ 配達件数＝ポイント合計 / 距離＝trip距離合計 / 配達時間＝trip時間合計 /
   クエスト＝重複排除後合計 / 調整＝符号付き合計 / 総売上＝配達報酬＋クエスト＋調整＋その他（一覧の合計表示・1日サマリー画面があれば照合）/
   UNKNOWNなし・日付不明なし・未分類なし・重複候補なし / チップ内訳（基本料金＋チップ＝最終売上）/ 個人情報 / 画像幅 / MAP / 既存データとの整合 / アプリのクエスト表示ロジックとの整合

11. 二重取込防止・既存データ保護:
   - 既存tripとは「時刻＋金額」で照合し既存IDを維持（新規は del_MMDD_(最大番号+1)）。既存値は上書きせず欠けている項目のみ補完。距離・時間・ポイントが既存と食い違えば停止。
   - 既存trip・クエスト・調整が一覧に見当たらない場合は削除せず停止。特別保証（guaranteeBonus）を含む日は自動取込対象外。
   - 同日を再取込して差分がなければ「変更なし（取込済み）」で何も書かない。経費・稼働セッション・vehicleType は触らない（取込では経費を入力しない）。
   - 反映した日には officialImport: { source: 'official-import-v1', importedAt } を付与。
   - 端末同期（js/store.js syncOfficialImportedDay）: officialImport のある日は公式データ（trip・件数・距離・クエスト・調整・売上）をシードに合わせ、○×評価・理由・経費・稼働セッションは保持、シードに無い配達記録（手動タップ等）は manualTapsArchive へ退避。
   - cloud-sync.js: officialImport のある日は古いクラウド売上で上書きされないよう、同梱の公式売上内訳を正とする。

12. MAP 処理（lib/crop_map.py）:
   - 既存規格 420x233 の地図領域を公式スクショから切り抜くのみ（生成・加工なし）。上端候補ごとに233行の窓を採点し、直後が白い行（地図下端）かつ幅420±3pxのものを採用。
   - 既存89件の元スクショで検証: 規格どおりに切り抜かれていた68件は既存box と±2px以内で一致。地図が画像端で切れている画像は推測せず停止（decisions.json の mapMissingOk で「MAPなし」登録を明示的に許可できる）。
   - 既存MAP・既存ファイルは上書きしない。map_<id>.png・full_<id>.png を assets/maps へ保存し TRIP_MAP_CATALOG に追加。

13. 将来の自動取得への拡張ポイント:
   - Uber Web 自動巡回で「一覧テキスト → inbox/<日付>/activity.txt」「各 View Details の Delivery詳細 → inbox/<日付>/screenshots/」を保存すれば、以降はこのパイプラインがそのまま受け口になる。
   - 一覧の View Details URL・trip UUID は解析済みで、スクショ側に tripUuid を記入すれば照合キーとして使用される。

14. テスト（node tools/official-import/import.js test・87項目 全PASS）:
   解析（Delivery／日付・時刻・金額の各表記／URL・UUID／タブ区切り／±調整／特別報酬・チップ・未分類検知）、クエスト重複排除（MISC+QUEST・¥0・時刻違い・重複候補・decisions）、
   MAP切り抜き（既存box一致・画像端で停止）、9/21・9/22再取込（PASS・変更なし・ファイル不変）、9/22 新規再構築（ID・No.・金額・距離・時間・ポイント・チップ・メモ・売上内訳・クエスト・調整・二重取込防止・アプリ計算）、
   既存日の補完（既存ID・○×評価・Bike経費・調整の公式名称/ユーザーメモ保持）、9/21 MAP新規切り抜き（停止→mapMissingOk で 6/7）、FAIL時に非反映（金額不一致・スクショ不足・UNKNOWN・番地・既存trip欠落・特別報酬・時刻不一致・チップ不一致）、
   端末同期（手動タップ退避・経費/稼働/○×保持・古いクラウド売上で上書きされない）、9/22時点確定値の回帰（9/22・今週・今月・平均・80件クエスト・MAP・確定週 ¥43,461）、本体データ整合性（全日）。
   ※ scratch/ の旧テスト群（Git管理外・過去バージョン時点の固定値や画面寸法のスナップショット）は 9/19 ¥320修正前・月間¥47,225 等の古い期待値で FAIL するが、日次取込のたびに月次値が変わるため固定値の更新はせず、
     同等の回帰確認は日付に依存しない形で spec/run-tests.js（10節: 9/22時点で凍結した確定値、11節: 全日の整合性）へ移した。
   ※ 既知の旧データ差異（変更していない）: 9/14 は officialPoints 5 / 配達件数 4 のまま確定済み（「配達件数＝ポイント合計」ルールは v21以降と公式取込日に適用）。月間距離の内部合算は 358.24km（旧記載 358.23km）。

15. 実装変更点（アプリ本体）:
   - js/store.js: syncOfficialImportedDay() 追加、シード補完ループで officialImport のある日を最優先で同期（9/21〜9/22 以前の日別分岐は変更なし）。
   - js/cloud-sync.js: officialImport 日の売上内訳ガードを追加。
   - バージョン: 20260923_v23（index.html / sw.js / version.json）。以降は apply 実行時に自動で +1。
   - 実績データ（CONFIRMED_SEED_DATA・TRIP_MAP_CATALOG・assets/maps）は今回変更なし（MAP 89/89 維持）。

■ v22 作業内容（2026-09-23 実装）: 9/22 公式売上調整 -¥607 追加

1. 追加した公式データ（Uber公式明細・一次情報）:
   - 日付/時刻: 2026-09-22 23:46
   - イベント種別: 調整 / 公式詳細表示: Support Adjustment
   - 金額: -¥607
   - 保存先: dailyLogs['2026-09-22'].adjustments[]（ID: adj_0922_1）
     { id:'adj_0922_1', time:'23:46', eventType:'調整', officialTitle:'Support Adjustment', amount:-607, userNote:'配達ミスに伴う調整', note }
   - 公式名称（officialTitle）は書き換えず、ユーザー説明「配達ミスに伴う調整」は別フィールド userNote に保持。
   - sales.adjustment = -607（調整イベント合計と一致）、sales.total = 9243。

2. 分類: 必要経費ではなく「売上調整」:
   - Uber側が売上に対して行った調整であり、バイクシェア／必要経費（log.expenses[]）には一切入れていない。
   - 9/18 の売上調整 +¥200 と同系統。分析上は「その他」へ符号付き（-¥607）で反映。
   - 「その他」はプラス／マイナス両方を扱う。同一集計期間内はネット集計（例: +¥200 - ¥607 = -¥407）。元データは調整イベント単位で別々に保持。

3. Delivery tripは変更なし:
   - 9/22の公式18trip（del_0922_1〜18）は削除・変更なし。
   - 12:53 del_0922_8（松のや 九条店・Delivery ¥607・3pt）と Support Adjustment -¥607 は同額だが別イベント。相殺・削除していない。

4. 9/22 修正後確定値:
   - 配達報酬 ¥8,250 ＋ クエスト ¥1,600 ＋ その他（売上調整）-¥607 ＝ 総売上 ¥9,243（旧 ¥9,850）
   - Bike -¥1,527 → 9/22売上利益 ¥7,716（旧 ¥8,323）
   - 配達25件 / 18トリップ / 67.13km / 7時間14分20秒（すべて変更なし）
   - 通常時給（調整込み通常売上 ¥9,243 ÷ 7.239h）: ¥1,277/h → 一覧表示 ¥1,280/h
   - 日別履歴の追加収支カード「売上調整金」はマイナス時 -¥607 表示に対応。日別属性「調」もマイナス調整日に付与。

5. 週次再計算（2026/9/21〜9/27、9/22時点）:
   - 配達報酬 ¥11,740 / クエスト ¥2,400 / 特別ボーナス ¥0 / その他 -¥607 / Bike -¥3,054 / 配達件数 35件
   - 今週売上 ¥4,290 ＋ ¥9,243 ＝ ¥13,533
   - 今週の売上利益 ¥13,533 － ¥3,054 ＝ ¥10,479（旧 ¥11,086）/ 先週比 -¥29,475

6. 80件クエスト: 35/80件（残45件）のまま。売上調整は配達件数に影響させない。

7. 月次再計算（2026年9月）:
   - 月間総売上 ¥47,225 ＋ ¥4,290 ＋ ¥9,243 ＝ ¥60,758（旧 ¥61,365）
   - 月間Bike ¥6,561 / 必要経費 ¥0 → 今月の売上利益 ¥54,197（旧 ¥54,804）
   - 配達件数 107件 / 公式トリップ 89 / MAP 89/89
   - 通常分析売上 ¥35,093 ＋ ¥4,290 ＋ ¥9,243 ＝ ¥48,626（旧 ¥49,233）
   - 平均日給 ¥48,626 ÷ 10日 ＝ ¥4,863 / 平均単価 ¥48,626 ÷ 107件 ＝ ¥454 / 平均時給 ¥1,227
   - 配達時間 39時間43分33秒（画面表示: 39時間43分）
   - 配達距離: 画面表示 358 km。内部合算は 358.24 km（日別シード距離の9/19までの合算が 263.19 km であり、旧HANDOFF記載 263.18 km と0.01km差。距離データは今回変更対象外のため未修正。表示値358kmに影響なし）

8. 実装変更点:
   - js/store.js: 9/22シードに adjustments[] と sales.adjustment -607 / total 9243 を追加。既存端末向け移行処理（9/22に調整イベントIDが無い場合のみ追加、sales.adjustment/total を公式値へ整合。二重追加なし）。
     getCalculatedMetrics の調整金判定を「> 0」から「!== 0」へ（マイナス調整も売上・通常分析売上に算入）。日別属性「調」も「!== 0」判定。
   - js/ui.js: 日別履歴の売上調整金カードをプラス／マイナス両対応。週次「その他」は既存でマイナス対応済み。
   - js/cloud-sync.js: adjustments[] をID基準でマージ保持。9/22保全ガードを ¥9,243（adjustment -607）へ更新し、旧クラウドの ¥9,850 で上書きされないよう公式値へ固定。
   - バージョン: 20260923_v22（index.html / sw.js / version.json）

9. 検証（scratch/test_v22_adjustment.js・89項目 全PASS）:
   - 新規端末（シード）/ v21端末からの移行 / 再ロード時の二重追加なし / cloud-syncマージ（旧クラウドが新しい updatedAt を持つ場合も ¥9,243 維持）
   - 回帰: 9/21公式データ、9/22 18trip、MAP 89/89、Bike ¥1,527×2日、9/18 +¥200、9/19 ¥21,310・del_0919_19、確定週 ¥43,461・利益 ¥39,954、80件クエスト 35/80、○×評価の保持
   - 旧テスト（test_v20_expense / test_919_repair / test_analytics_sales_profit）はv21以前の期待値を固定で持つためHEAD時点から既にFAIL（今回の変更による回帰ではないことをHEADで同一結果として確認済み）。

■ 分類ルール（今後も厳守）:
   - Uber側の売上調整（Support Adjustment 等、プラス／マイナスとも）→ 売上の「その他」（dailyLogs[date].adjustments[] ＋ sales.adjustment）
   - ユーザー自身が支払ったBike・修理・備品等 → 経費（log.expenses[]：バイクシェア／必要経費）
   - 同額の Delivery trip と調整イベントを同一視・相殺しない。

■ v21 作業内容（2026-09-23 実装）:

1. 2026-09-21 公式確定実績の追加（公式スクリーンショット7枚と100%照合）:
   - 配達10件 / 公式7トリップ / 公式ポイント10pt
   - 配達報酬 ¥3,490 ＋ クエスト ¥800 ＝ 総売上 ¥4,290
   - 公式配達距離 27.92 km（明細合算＝公式値一致）
   - 公式配達時間 2時間48分47秒（明細合算＝公式値一致）
   - Bike経費 -¥1,527 → 9/21利益 ¥2,763
   - trip ID: del_0921_1 〜 del_0921_7
   - クエスト: 13:10「クエスト ¥800」「6回乗車クエスト ¥800」は同一報酬の重複表示のため¥800を1回のみ計上（quest_0921_1）。二重計上なし。

2. 2026-09-22 公式確定実績の追加（公式スクリーンショット18枚と100%照合）:
   - 配達25件 / 公式18トリップ / 公式ポイント25pt
   - 配達報酬 ¥8,250 ＋ クエスト ¥1,600 ＝ 総売上 ¥9,850
   - 公式配達距離 67.13 km（明細合算＝公式値一致）
   - 公式配達時間 7時間14分20秒（明細合算＝公式値一致）
   - Bike経費 -¥1,527 → 9/22利益 ¥8,323
   - trip ID: del_0922_1 〜 del_0922_18
   - クエスト: 14:29・20:09の各「クエスト ¥800」「6回乗車クエスト ¥800」はそれぞれ同一報酬の重複表示のため各¥800を1回のみ計上（quest_0922_1 / quest_0922_2、合計¥1,600）。15:00の「3回乗車クエスト ¥0」は報酬0のため売上非加算（記録はquest_0922_2のnoteに保持）。
   - チップの扱い: 10:50 マクドナルド 九条店（del_0922_6）は 基本料金¥320 ＋ チップ¥50 ＝ 最終売上¥370。
     fee には最終売上 ¥370 のみを計上（二重加算なし）。内訳は baseFee:320 / tip:50 フィールドとmemoに保持。
   - 公式表記の相違: 17:48のピザハットは公式表記「ピザハット 大阪ナインモール九条店」（作業指示の「イオンモール九条店」ではない）。公式を正とした。
   - 08:44（del_0922_2）の配達先は公式表記が「1-chōme Osaka Osaka Shinmachi Japón」のローマ字表記のみ。区名は推測せず area は「大阪市 新町1丁目」、公式原文は memo に転記。

3. バイクシェア経費:
   - 9/21: exp_0921_1「ドコモ・バイクシェア1日パス」¥1,527
   - 9/22: exp_0922_1「ドコモ・バイクシェア1日パス」¥1,527
   - 既存の 9/18 exp_0918_1 ¥1,980 / 9/19 exp_0919_1 ¥1,527 は一切変更せず、二重計上なし（自動テストで確認）。
   - ※料金根拠: 既存データ上の1日パスは 9/18 ¥1,980 / 9/19 ¥1,527 と2通り存在し、料金一定とは確認できなかったため、ユーザー確認のうえ各日¥1,527で登録した。

4. 80件クエスト進捗の公式補正（期間: 2026/9/21 04:00〜9/25 04:00）:
   - 進捗判定を「公式トリップはポイント数＝配達件数」で集計するよう countQuestDeliveries() を新設（ダブル・トリプル配達を正しく件数換算）。
   - 現在値: 35 / 80件（残り45件）。
   - 端末に残る旧手動オフセット（manualAdjust）は公式件数と二重計上になるため、QUEST_OFFICIAL_BASELINE = '20260922_official' と一致する保存データのみ有効として扱う（旧オフセットは自動的に無効化）。ユーザーの±ボタン操作は従来どおり有効。
   - 手動配達ログは削除しない: 公式トリップ置換時、端末の手動タップ記録は log.manualTapsArchive[] へ退避して保全する。

5. 公式MAP画像:
   - 公式スクリーンショット（C:\Users\okano\Pictures\Screenshots\スクリーンショット 2026-09-22 2108〜2117台）を日付・時刻・金額・距離・時間で厳密照合。
   - 25件すべて full_*.png 保存＋MAP領域クロップ（420x233）で map_*.png 生成、js/trip-maps.js の TRIP_MAP_CATALOG へ登録。
   - MAP登録数: 64件 → 89件（公式トリップ89件中89件 = 100%）。捏造・生成MAPは一切なし。

6. 経費機能の同期安全性強化（v20機能の補強）:
   - 経費削除時に log.deletedExpenseIds[]（tombstone）を記録。クラウド同期のID統合および公式シード補完で削除済み経費が復活しないようにした。
   - 経費の日付変更は「旧日付から削除 → 新日付へ同一IDのまま追加」に変更し、同期時の重複発生を防止。
   - 内容欄（自由入力）のHTMLエスケープを追加。未来日は登録・修正の双方で拒否。
   - 修正／削除ボタンを高さ36px・間隔12pxへ拡大（スマホ誤タップ防止）。
   - cloud-sync.js に 9/21・9/22 公式売上（¥4,290 / ¥9,850）の保全ガードを追加（9/19の¥21,310ガードと同方式）。

7. 検証結果（自動テスト・全PASS）:
   - 9/21・9/22の件数／トリップ数／報酬／クエスト／総売上／距離／時間／ポイント／利益が公式確定値と完全一致。
   - 既存回帰: 9/19（19トリップ・23配達・¥21,310・del_0919_19存在）、9/18（¥5,067・Bike¥1,980）、確定週9/14〜9/20（売上¥43,461・Bike¥3,507・利益¥39,954、差額¥0）を維持。
   - 端末に手動タップが残る状態からの移行、○×評価・MAP一口メモ・稼働セッションの保全、削除経費の非復活、クラウドマージ（公式データ優先・tombstone維持）を確認。

■ 今回の9/19欠落トリップ確認および修復実装内容:
1. 差額¥320の原因解明と一次情報確認:
   - これまで残っていたUber公式週次金額（¥43,461）とUBER_LOG詳細積み上げ（¥43,141）の差額 ¥320 の原因を調査。
   - 9/19の公式トリップ「11:44 スシロー 辰巳橋店 → 大阪市西区本田2丁目（1.98km、13分34秒、報酬¥320、1pt）」がUBER_LOGへの初回取り込みから漏れていたことを特定。
   - 一次情報となるUber公式画面のスクリーンショット（C:\Users\okano\Pictures\Screenshots\スクリーンショット 2026-09-21 071211.png）が存在し、公式明細と100%合致することを確認。
   - 推測補完ではなく、確認済みの公式トリップとして正式追加・修復を実施。

2. 追加した公式tripの詳細:
   - 日付: 2026-09-19
   - 完了時刻: 11:44
   - 店舗: スシロー 辰巳橋店 (Sushiro TATSUMIBASHI)
   - 配達先: 大阪市西区本田2丁目
   - 報酬: ¥320
   - 所要時間: 13分34秒（814秒）
   - 距離: 1.98 km
   - ポイント: 1
   - ID: del_0919_19 (時系列位置: No.10)

3. 9/19実績の再計算・確定値:
   - 公式トリップ数: 18トリップ → 19トリップ
   - 配達件数: 23件（ダブル配達含むため件数は23件のまま変更なし）
   - 通常報酬: ¥7,908 → ¥8,228 (+¥320)
   - クエスト: ¥950（変更なし）
   - 新規保証: ¥12,132（変更なし）
   - 9/19総売上: ¥21,310（通常報酬¥8,228 ＋ クエスト¥950 ＋ 新規保証¥12,132）
   - Bike経費: -¥1,527（変更なし）
   - 9/19利益: ¥19,783（総売上¥21,310 － Bike¥1,527）
   - 配達距離: 68.20 km → 70.18 km
   - 配達時間: 6時間17分12秒 → 6時間30分46秒（画面表示: 6時間30分）
   - 公式ポイント合計: 22 → 23
   - 9/19時給: 通常分析売上¥9,178 ÷ 6.5128時間 ＝ ¥1,409.23/h → 10円丸め ¥1,410/h（新規保証は時給計算から除外）

4. 週次差額 ¥320 の完全解消:
   - UBER_LOG週次内部合計: ¥43,141 ＋ ¥320 ＝ ¥43,461
   - Uber公式週次明細: ¥43,461
   - UBER_LOG詳細積み上げ ＝ Uber公式週次明細（¥43,461）が数学的に完全一致。
   - これまでHANDOFFに保持していた「未解決差額 ¥320」は【完全解決（差額¥0）】へステータス変更。

5. 週次売上利益の修正:
   - 確定週（9/14〜9/20）公式売上: ¥43,461
   - Bike経費合計: ¥3,507（9/18: ¥1,980 ＋ 9/19: ¥1,527）
   - 週次売上利益: ¥43,461 － ¥3,507 ＝ ¥39,954（従来 ¥39,634 → ¥39,954）
   - 9/21以降の「先週比」等も先週利益 ¥39,954 を基準に再計算。

6. 月次実績の再計算（2026年9月度累計）:
   - 月間総売上: ¥46,905 ＋ ¥320 ＝ ¥47,225
   - 月間Bike経費: ¥3,507
   - 現行定義の月間売上利益: ¥47,225 － ¥3,507 ＝ ¥43,718（従来 ¥43,398 → ¥43,718）
   - 配達件数: 72件（変更なし）
   - 通常分析売上（特別保証除外）: ¥34,773 ＋ ¥320 ＝ ¥35,093
   - 平均日給: ¥35,093 ÷ 8稼働日 ＝ 4,386.63円 → ¥4,387（従来 ¥4,347）
   - 平均単価: ¥35,093 ÷ 72件 ＝ 487.40円 → ¥487（従来 ¥483）
   - 配達時間: 29時間40分（従来 29時間26分）
   - 配達距離: 263 km（内部値: 263.18 km, 従来 261 km）

7. 公式MAP画像の保存と登録:
   - 公式スクリーンショットより assets/maps/full_del_0919_19.png を保存。
   - 地図部分を正確にクロップ（box: [13, 167, 433, 400], 420x233）し assets/maps/map_del_0919_19.png を生成。
   - js/trip-maps.js の TRIP_MAP_CATALOG に del_0919_19 を登録。全64公式トリップ中64件（100%）登録完了。

8. 既存ユーザーデータとID紐付けの完全保護:
   - 既存のdel_0919_1〜del_0919_18のIDは一切リネームせず保持。
   - 新規tripは id: "del_0919_19" を付与し、時系列（11:44）に沿って No.10 として挿入。以降のNo.（index）を11〜19に自然整列。
   - ○×評価、評価理由、MAP一口メモ、履歴ソート、今週クエスト進捗、日別属性、既存63件のMAPカタログとの紐付けは完全に維持。

9. 全画面幅（375px / 390px / 414px / 430px）のレイアウト検証:
   - 375px（iPhone SE）を含むすべての幅において、オレンジ枠が下段まで完全に囲い、2列×4段が崩れず、文字の折り返し・はみ出し・横スクロールがゼロであることを確認。

■ v20 新機能：下部ナビ名称変更＋経費入力機能（2026-09-21 14:30実装）:

1. 下部ナビ「今日」→「稼働」名称変更:
   - 下部ナビ4タブの第1タブ表示名を「今日」から「稼働」へ変更。
   - data-tab="today" 等の内部IDは変更なし（後方互換維持）。

2. 経費入力インラインセクション（稼働画面下部）:
   - 入力項目: 日付（過去日変更可、未来日不可）、種類（バイクシェア/必要経費の2分類）、内容（自由入力・最大60文字）、金額（正の整数のみ、0や不正文字は拒否）。
   - ［登録］ボタンで経費をlog.expenses[]配列に保存（既存データ構造を再利用、別構造は作成していない）。
   - 登録済み経費リスト: 直近14日分を日付降順で表示。各経費に［修正］［削除］ボタンあり。
   - 修正: インライン展開フォームで日付・種類・内容・金額すべて変更可能。日付変更時は旧日付からdelete→新日付にadd。
   - 削除: confirm()確認ダイアログ付き。

3. 経費データ構造:
   - 各レコード: { id, category, amount, memo, createdAt, updatedAt }
   - id: exp_[timestamp]_[random4char] 形式（一意）
   - category: "バイクシェア" or "必要経費"（新規登録時）/ 既存データは "バイクシェア利用" 等
   - Uber公式売上データとは完全分離（log.expenses[]に保存）

4. 分析画面との連動:
   - バイクシェア: 既存Bike検出ロジック（cat.includes('バイク') || cat.includes('サイクル') || cat.includes('bike')）により週次Bike・月次Bikeに自動集計。
   - 必要経費: 月次分析の「必要経費」カードへ反映（getRevenueSummary内でcat === '必要経費'判定）。月次利益 = 売上 - Bike - 必要経費。
   - 週次売上利益には必要経費を含めない（ユーザーと後日決定予定）。

5. 既存データ保護:
   - 9/18 exp_0918_1（バイクシェア利用 ¥1,980）、9/19 exp_0919_1（バイクシェア利用 ¥1,527）は一切変更なし。
   - 既存カテゴリ「バイクシェア利用」はBike検出ロジックにより正常にBikeとして検出される。二重計上なし。
   - cloud-sync.jsの経費マージ（ID基準）は変更なし。

6. 未決定事項:
   - 必要経費を週次売上利益から差し引くかどうかは未決定（ユーザーと後日協議）。v21時点でも週次利益には含めていない（月次のみ反映）。

--------------------------------------------------------------------------------
1. プロジェクト概要・アーキテクチャ
--------------------------------------------------------------------------------
UBER_LOGは、Uber Eats 配達員向けの実働・売上・配達効率・地雷スポット管理を行うモバイルファーストPWAです。

- 技術スタック:
  - フロントエンド: Vanilla HTML5 / CSS3 / ES6+ JavaScript（ビルド不要）
  - オフライン & 自動更新: Service Worker（sw.js, キャッシュ名: uber-log-20260925_v38）
  - バージョン管理: version.json + window.UBER_LOG_APP_VERSION = '20260925_v38'（公式取込 apply 時に自動で +1）
  - データ永続化: localStorage（キー: uber_log_v1_data, uber_log_trip_map_memos, uber_log_history_sort_order, uber_log_quest_*） + Supabase クラウド同期（cloud-sync.js）
  - デザイン思想: iPhone縦画面（幅375〜430px）最優先のハイコントラスト・ダークモダンUI

- 主要ファイル構成:
  - index.html: 4大タブ構造（稼働/履歴/分析/地雷）、経費入力インラインセクション（日付変更可・バイクシェア/必要経費の2分類・登録/修正/削除対応）、今週の売上利益カード（緑枠・2列3段6項目）、今月の実績カード（オレンジ枠・売上利益看板・2列4段8項目）、今週のクエスト進捗カード、Service Worker自動登録・更新監視
  - sw.js: Service Worker（SW_VERSION: 20260925_v38）
  - version.json: 公開版バージョンメタデータ（version: 20260925_v38）
  - tools/official-import/: 公式取込 v1（PC用取込補助画面・CLI・解析/照合/検証/反映・MAP切り抜き・テスト。inbox/・staging/ はGit管理外）
  - .claude/commands/uber-import.md: /uber-import YYYY-MM-DD（毎日の公式取込コマンド）
  - デスクトップ「UBER_LOG」→ tools/official-import/launcher.vbs → server.js（127.0.0.1:8088）→ 公式取込画面［取り込み実行］（v26 の日常入口）
  - tools/official-import/run-latest-import.ps1: 保守用（v25 の UBER取込.cmd は v26 で削除）
  - manifest.json: PWA定義（start_url: ./index.html, scope: ./, display: standalone）
  - js/app.js: メインコントローラー（フォアグラウンド復帰時の自動バージョンチェック、復旧用ボタン制御、経費入力イベント初期化）
  - js/store.js: データストア（公式シードデータ、自動マイグレーション、メトリクス計算、銀行営業日振込日動的算出、特別ボーナス除外平均計算、クエスト重複排除、日別比較データ生成、MAP一口メモ永続化、曜日色統一、日別属性定義、履歴ソート管理、週クエスト進捗管理、週次売上利益計算、月次売上利益計算、Bike経費集計、経費CRUD（addExpense/updateExpense/deleteExpense/getRecentExpenses）、月次必要経費集計）
  - js/ui.js: UIコントローラー（今週の売上利益描画、今月の実績看板・8カード描画、昨日比カウント整列描画、全幅稼働開始ボタン描画、分析タブV2描画、新1行日付バーアコーディオン、+N集約バッジ、基本セット統一、追加収支カード、single-open制御、No.表記、MAPモーダル制御、Google Maps URL生成、○×評価・理由メモ、月間距離整数表示、今週のクエスト進捗描画、履歴配達明細ソート切替描画、経費入力セクション描画・修正フォーム・削除確認・分析再描画連動）
  - js/trip-maps.js: Uber公式トリップ地図画像カタログ（TRIP_MAP_CATALOG: 全109件完全登録済み）
  - js/cloud-sync.js: クラウド同期・マージ制御（特別保証保全ガード・確定値保護、9/21 ¥4,290・9/22 ¥9,243（調整-¥607込み）公式売上ガード、売上調整イベントadjustments[] ID基準マージ、経費明細ID基準マージ＋削除tombstone、手動タップアーカイブ保持）
  - css/app.css: スタイルシート（今週の売上利益カード、今月の実績オレンジ枠カード、8項目2列4段グリッド、.val-bike赤色スタイル、「配」バッジ淡い蛍光ピンク、日別属性24px、trip-points-pill控えめインライン表示、今週のクエスト進捗カード、履歴配達明細ソート切替ボタン、昨日比数字整列、全幅稼働開始ボタン、昨日売上18px、内訳対角21px、今月実績対角22px、今月売上強調廃止・統一、新1行日付バー、曜日通常色統一、経費入力セクション・修正フォーム・カテゴリバッジ）
  - assets/maps/: クロップ済み公式実績地図（map_*.png: 109件）および元スクリーンショット（full_*.png: 109件）

--------------------------------------------------------------------------------
2. データ思想と基本原則（最重要・絶対厳守）
--------------------------------------------------------------------------------
1. 第1基準はUber公式明細・公式スクリーンショット（一次情報）:
   - 売上・トリップ・報酬・距離・所要時間・地図は、Uber公式明細および公式スクリーンショットを唯一の真実（Source of Truth）とする。
   - 推測値、架空明細、合計金額からの逆算、別日データのコピーは厳禁。
2. 未解決差額（¥320）問題の解決:
   - 9/19の11:44公式trip（¥320）の欠落が一次情報スクリーンショットにより確認され、正式登録されたことで、公式週明細（¥43,461）とUBER_LOG週次集計（¥43,461）が完全一致し、差額は解消した。
3. トリップ数と配達件数（ポイント）の厳格な概念分離:
   - 例: 2026-09-19は 19公式トリップ（カード19枚） / 23配達（23pt）。ダブル配達による件数差を水増ししない。
4. プライバシー保護と本人経験メモの厳格な切り分け:
   - 注文者名、部屋番号、電話番号等の個人情報は一切保持・自動転記しない。
   - 建物名、階段、ドロップ難易度、ルート注意点等の「配達員自身の現場経験」のみを記録する。
5. 効率指標における大型特別ボーナスの除外:
   - 平均日給、平均単価、通常時給は、通常稼働売上（通常報酬＋通常クエスト＋調整金）を基準に計算し、新規保証等の大型特別ボーナスと特別クエスト（questType: special。v37〜）は含めない。
   - 総売上および総利益には特別ボーナスを含める。
6. Uber売上調整と経費の厳格な分離:
   - Uber側の売上調整（例: 9/18 +¥200、9/22 23:46 Support Adjustment -¥607）は売上の「その他」へ符号付きで反映し、調整イベント単位で adjustments[] に保持する。公式名称は書き換えない。
   - ユーザー自身が支払ったBike・修理・備品等のみが経費（バイクシェア／必要経費）。売上調整を経費へ入れない。
   - 同額の Delivery trip と調整イベントは別イベントとして扱い、相殺・削除しない。
8. 経費などユーザー入力は、公式取込・公式シード補完・クラウド同期のいずれでも消さない（v27）:
   - 経費は日別ログ全体の新旧で勝ち負けさせず、必ず経費ID単位で統合（片方だけの経費は残す／同IDは updatedAt の新しい方／deletedExpenseIds は和集合）。
   - 端末保存・クラウド送信はどちらも「最新の保存内容と統合してから書く」。丸ごと上書きしない。
7. 毎日の公式データ更新は「公式取込 v1」（/uber-import YYYY-MM-DD）を使う:
   - 検証 FAIL 時に本体データを手で直して通さない。ズレた項目を報告して止める。
   - 経費（Bike・必要経費）は取込では入力せず、アプリの稼働画面の経費入力を使う（Uber売上と経費は別データソース）。
9. 稼働日の共通定義（v38）:
   - 稼働日 = 公式の配達件数／トリップ数が1以上、実配達（店舗名または報酬額のある Delivery）が1件以上、または配達売上 > 0。判定は store.isWorkedDay に一本化。
   - 稼働開始時刻・workSession・手動タップ・経費・同期データだけの日、空レコード、0件/売上0の日は非稼働。日別履歴一覧・日別比較に出さない（データは削除しない）。
   - 「今日」かどうか・レコードの有無では判定しない。確認済みの非稼働日（9/12・9/13・9/20）はカレンダーで「休」。
10. バイクシェア1日パスの自動入力額は ¥1,527（v38 正式。v32 の ¥527 は誤設定）。登録済みの経費は書き換えない。
11. 日別履歴の特別ボーナス系アイコンは 🏆 に統一（v38〜。新規保証・特別収入／特別クエスト。1日1個）。内部データの分類は別のまま。

--------------------------------------------------------------------------------
3. 日別実績確定データ一覧（2026年9月度・全11稼働日）
--------------------------------------------------------------------------------
- 2026-09-10 (木): 配達 3件 (2トリップ) | 売上 ¥1,353 | 配達時間 1時間24分5秒 | 走行 14.3km | 利益 ¥1,353 | MAP 2/2件
- 2026-09-11 (金): 配達 5件 (4トリップ) | 売上 ¥2,411 | 配達時間 1時間55分45秒 | 走行 17.5km | 利益 ¥2,411 | MAP 4/4件
- 2026-09-14 (月): 配達 4件 (4トリップ) | 売上 ¥2,443 | 配達時間 5時間34分42秒 | 走行 17.9km | 利益 ¥2,443 | MAP 4/4件
- 2026-09-15 (火): 配達 11件 (11トリップ) | 売上 ¥5,632 | 配達時間 4時間38分33秒 | 走行 41.8km | 利益 ¥5,632 | MAP 11/11件
- 2026-09-16 (水): 配達 8件 (8トリップ) | 売上 ¥4,802 | 配達時間 3時間53分40秒 | 走行 38.3km | 利益 ¥4,802 | MAP 8/8件
- 2026-09-17 (木): 配達 7件 (7トリップ) | 売上 ¥4,207 | 配達時間 2時間36分47秒 | 走行 28.5km | 利益 ¥4,207 | MAP 7/7件
- 2026-09-18 (金): 配達 11件 (9トリップ) | 売上 ¥5,067 | 配達時間 3時間06分13秒 | 走行 34.6km | 利益 ¥3,087 (バイクシェア-¥1,980) | [B] | MAP 9/9件
- 2026-09-19 (土): 配達 23件 (19トリップ) | 売上 ¥21,310 | 配達時間 6時間30分46秒 | 走行 70.18km | 利益 ¥19,783 (バイクシェア-¥1,527) | [B][🏆] | MAP 19/19件
  ※9/19売上内訳: 通常報酬 ¥8,228 ＋ クエスト ¥950 ＋ 特別保証 ¥12,132 ＝ ¥21,310
- 2026-09-21 (月): 配達 10件 (7トリップ) | 売上 ¥4,290 | 配達時間 2時間48分47秒 | 走行 27.92km | 利益 ¥2,763 (バイクシェア-¥1,527) | [B] | MAP 7/7件
  ※9/21売上内訳: 配達報酬 ¥3,490 ＋ クエスト ¥800 ＝ ¥4,290（6回乗車クエスト重複表示は1回のみ計上）
- 2026-09-22 (火): 配達 25件 (18トリップ) | 売上 ¥9,243 | 配達時間 7時間14分20秒 | 走行 67.13km | 利益 ¥7,716 (バイクシェア-¥1,527) | [B][調] | MAP 18/18件
  ※9/22売上内訳: 配達報酬 ¥8,250 ＋ クエスト ¥1,600 ＋ その他（売上調整）-¥607 ＝ ¥9,243
  ※23:46 調整「Support Adjustment」-¥607（adj_0922_1、経費ではなく売上調整。ユーザーメモ: 配達ミスに伴う調整）
  ※クエスト内訳: ¥1,600（¥800×2回。各回の6回乗車クエスト重複表示は計上せず、15:00の3回乗車クエスト¥0は非加算）
  ※配達報酬¥8,250には10:50 del_0922_6 のチップ¥50を含む最終売上¥370を含む（基本料金¥320＋チップ¥50、二重加算なし）
- 2026-09-23 (水): 配達 23件 (20トリップ) | 売上 ¥10,683 | 配達時間 7時間9分33秒 | 走行 65.32km | 経費: アプリ入力分（Bike経費をアプリで登録済み・金額はHANDOFF未把握） | [B] | MAP 20/20件 | 公式取込v1（officialImport）
  ※9/23売上内訳: 配達報酬 ¥9,033 ＋ クエスト ¥1,650 ＋ 調整 ¥0 ＝ ¥10,683
  ※クエスト内訳: 13:06 ¥900（「6 回乗車クエスト ¥900」重複表示は計上せず）＋ 14:05 ¥750（「3 回乗車クエスト ¥750」重複表示は計上せず）。15:00「3 回乗車クエスト ¥0」は非加算
  ※14:53 del_0923_17 ローソン 靱本町三丁目: 最終売上¥538（基本料金¥320＋チップ¥218、二重加算なし）
  ※14:40 del_0923_16 の配達先は公式表記がローマ字のみ（2-chōme Osaka Osaka Enokojima 日本）。区名は推測せず area「大阪市江之子島2丁目」、原文は memo に転記
  ※trip ID: del_0923_1〜20 / 10:54・13:14・14:53 の3件は初回スクショで地図が切れていたため撮り直し後に取込

【確定週合計（9/14〜9/20）】:
- 配達件数: 64件 (トリップ数: 58件)
- 売上内訳: 配達報酬 ¥27,329 ＋ クエスト ¥3,800 ＋ 特別ボーナス ¥12,132 ＋ その他 +¥200 ＝ 売上 ¥43,461
- Bike経費: -¥3,507 (9/18: ¥1,980 ＋ 9/19: ¥1,527)
- 今週の売上利益: ¥39,954 (売上 ¥43,461 － Bike ¥3,507)
- 公式週明細: ¥43,461（差額: ¥0 完全一致・解消済み）

【進行中の週（9/21〜9/27）: 9/23時点】:
- 配達件数: 58件 (トリップ数: 45件)
- 売上: ¥24,216（配達報酬 ¥20,773 ＋ クエスト ¥4,050 ＋ 特別ボーナス ¥0 ＋ その他 -¥607）
- Bike経費: -¥3,054 (9/21: ¥1,527 ＋ 9/22: ¥1,527) ※9/23はアプリ入力分（HANDOFF未把握・未計上）
- 今週の売上利益（同梱データ分）: ¥21,162（売上 ¥24,216 － Bike ¥3,054）※9/23の経費入力があればアプリ表示はその分少なくなる
- 振込予定: 9月29日（火）
- ※必要経費は週次売上利益に含めない（未決定事項のため現状維持）

【80件クエスト進捗（2026/9/21 04:00〜9/25 04:00）】:
- 58 / 80件（残り22件・達成率73%）※公式確定配達数ベース（9/21 10件 ＋ 9/22 25件 ＋ 9/23 23件）

【月累計（9/10〜9/23）】:
- 配達件数: 130件 (トリップ数: 109件)
- 売上: ¥71,441（通常稼働売上: ¥59,309）
- 今月の売上利益（同梱データ分）: ¥64,880（売上 ¥71,441 － バイクシェア ¥6,561 － 必要経費 ¥0）※9/23の経費はアプリ入力分（HANDOFF未把握・未計上）
- バイクシェア経費: ¥6,561 (9/18 ¥1,980＋9/19 ¥1,527＋9/21 ¥1,527＋9/22 ¥1,527) / 必要経費: ¥0
- 平均日給: ¥5,392（11稼働日）/ 平均単価: ¥456 / 平均時給: ¥1,268
- 配達時間: 46時間53分（正確値 46時間53分6秒）
- 配達距離: 424 km（内部値: 423.56 km）
- 月間「その他」（売上調整ネット）: +¥200（9/18）－ ¥607（9/22）＝ -¥407
- MAP登録数: 109/109件 (100%)
================================================================================
